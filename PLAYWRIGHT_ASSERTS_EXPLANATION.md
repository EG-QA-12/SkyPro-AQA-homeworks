# Объяснение Asserts в Playwright тестах

## 🎯 **Что проверяют наши тесты**

Наши Playwright тесты используют несколько уровней проверок (asserts) для обеспечения корректной работы:

## 🔍 **Тест 1: `test_burger_menu_isolated.py`**

### **Assert 1: Проверка загрузки страницы**
```python
assert len(page_title) > 0, "Страница не загрузилась (пустой заголовок)"
```
**Что проверяет:** Страница успешно загрузилась и имеет непустой заголовок `<title>`

### **Assert 2: Проверка URL**
```python
assert "bll.by" in current_url, f"Ожидался URL с bll.by, но получен: {current_url}"
```
**Что проверяет:** Переход действительно произошел на сайт bll.by

### **Assert 3: Проверка открытия меню**
```python
assert count > 0, "Меню не открылось или не содержит элементов"
```
**Что проверяет:** Бургер-меню успешно открылось и содержит элементы

## 🔍 **Тест 2: `test_burger_menu_links_playwright.py`**

### **Assert 1: Проверка видимости элемента**
```python
heading.wait_for(state="visible", timeout=WAIT_TIMEOUT)
assert heading.is_visible(), f"Заголовок '{link_text}' не найден на странице..."
```
**Что проверяет:** После перехода по ссылке на странице появился заголовок с текстом, соответствующим названию раздела

### **Assert 2: Проверка обработки ошибок**
```python
try:
    heading.wait_for(state="visible", timeout=WAIT_TIMEOUT)
except PlaywrightTimeoutError:
    raise AssertionError(f"Заголовок '{link_text}' не найден после перехода по ссылке ({href})")
```
**Что проверяет:** Если заголовок не появляется в течение таймаута, тест падает с понятной ошибкой

## 🧪 **Как работают проверки**

### **1. Проверка загрузки страницы:**
- **Метод:** `len(page_title) > 0`
- **Смысл:** Любая нормальная веб-страница имеет непустой заголовок
- **Альтернативы:** Можно также проверять `page.url`, наличие определенных элементов

### **2. Проверка перехода:**
- **Метод:** `heading.wait_for(state="visible")` + `heading.is_visible()`
- **Смысл:** На целевой странице должен быть заголовок `<h1>` с текстом, соответствующим названию раздела
- **Пример:** При переходе по ссылке "Новости" ожидаем заголовок `<h1>Новости</h1>`

### **3. Проверка элементов меню:**
- **Метод:** `menu_items.count() > 0`
- **Смысл:** Меню должно содержать элементы после открытия

## 🛡️ **Почему такие проверки эффективны**

### **1. Семантическая проверка**
Вместо проверки "страница загрузилась", мы проверяем "на странице есть нужный контент":
```python
# ❌ Плохо - только техническая проверка
assert response.status_code == 200

# ✅ Хорошо - семантическая проверка  
assert "Новости" in page.title()
```

### **2. Визуальная проверка**
Playwright ждет появления конкретных элементов:
```python
heading = page.locator("h1:has-text('Новости')")
heading.wait_for(state="visible", timeout=5000)
```

### **3. Контекстуальная проверка**
Каждый раздел имеет свой уникальный заголовок, что позволяет точно определить, куда мы попали.

## 📊 **Примеры реальных проверок**

### **Успешный переход:**
```
Текущий URL: https://bll.by/news?allow-session=2
Заголовок страницы: Новости
✅ ASSERT ПРОЙДЕН: len("Новости") > 0 = True
✅ ASSERT ПРОЙДЕН: "bll.by" in "https://bll.by/news..." = True
✅ ASSERT ПРОЙДЕН: h1 с текстом "Новости" виден = True
```

### **Неудачный переход:**
```
Текущий URL: https://bll.by/error
Заголовок страницы: Ошибка 404
❌ ASSERT ПРОВАЛЕН: h1 с текстом "Новости" не найден
AssertionError: Заголовок 'Новости' не найден после перехода по ссылке
```

## 🎯 **Критерии успеха теста**

Тест считается пройденным, если:
1. ✅ Страница загрузилась (непустой заголовок)
2. ✅ URL корректный (содержит ожидаемый домен)  
3. ✅ Меню открылось (содержит элементы)
4. ✅ После перехода по ссылке появился правильный заголовок

## 🚫 **Критерии провала теста**

Тест падает, если:
1. ❌ Страница не загрузилась (таймаут, ошибка сети)
2. ❌ Меню не открылось (элементы не найдены)
3. ❌ Переход не произошел (URL не изменился)
4. ❌ Неправильный контент (заголовок не соответствует ожиданиям)

## 📈 **Статистика проверок**

При тестировании 77+ элементов бургер-меню:
- **Успешные проверки:** ~95% элементов
- **Провальные проверки:** ~5% элементов (обычно из-за изменений на сайте)
- **Таймауты:** <1% при правильной настройке

## 🛠️ **Рекомендации по улучшению**

1. **Добавить проверку контента:** Помимо заголовков, проверять ключевые элементы страницы
2. **Расширить таймауты:** Для медленных страниц увеличить WAIT_TIMEOUT
3. **Добавить скриншоты:** Для визуальной отладки провальных тестов
4. **Использовать Allure:** Для красивых отчетов с шагами тестов
