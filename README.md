# BLL Tests: Фреймворк автоматизации тестирования

✨ **НОВАЯ АРХИТЕКТУРА** - Проект был полностью реструктурирован для лучшей организации и масштабируемости!

## 🏗️ Структура проекта

```
Bll_tests/                              # Корневая директория
├── framework/                          # Общий фреймворк автоматизации
│   ├── utils/                         # Общие утилиты (auth_utils, allure_utils)
│   ├── fixtures/                      # Переиспользуемые фикстуры
│   └── app/                           # Специфичный код приложения
├── tests/                             # Тестовые директории
│   ├── e2e/                          # E2E тесты
│   └── integration/                   # Интеграционные тесты
├── projects/                          # Вложенные проекты
│   └── auth_management/               # Проект авторизации
├── scripts/                           # Общие скрипты
├── docs/                             # Документация
├── config/                           # Конфигурация фреймворка
│   ├── pytest/                       # Настройки pytest
│   ├── allure/                        # Настройки allure
│   └── browsers/                      # Настройки браузеров
├── secrets/                          # Секретные данные (в .gitignore)
│   ├── .env                          # Переменные окружения
│   ├── bulk_users.csv                # CSV с данными пользователей
│   ├── creds.env                     # Креды
│   └── test_users.csv                # Тестовые пользователи
└── cookies/                          # Файлы cookies (в .gitignore)
    ├── admin_cookies.json
    ├── user_cookies.json
    ├── moderator_cookies.json
    └── [другие cookie файлы]
```

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
# Установка зависимостей фреймворка
pip install -r requirements.txt

# Установка браузеров для тестирования
playwright install
```

### 2. Запуск тестов
```bash
# E2E тесты
pytest tests/e2e/ -v --alluredir=allure-results

# Интеграционные тесты
pytest tests/integration/ -v --alluredir=allure-results

# Отчет Allure
allure serve allure-results
```

### 3. Настройка секретов
1. Скопируйте `secrets/.env.example` в `secrets/.env`
2. Заполните необходимые переменные окружения
3. Убедитесь, что файлы секретов не попадают в git (.gitignore)

## 🔐 Массовая авторизация пользователей

Фреймворк включает **оптимизированный набор из 4 ключевых инструментов** для массовой авторизации:

### 🚀 Простое решение (90% случаев)
```bash
# Двойной клик в Windows
projects/auth_management/run_bulk_auth_headless.bat
```
**Назначение:** Быстрая авторизация до 100 пользователей в headless режиме

### 🎯 Гибкое решение (кастомизация)
```bash
# Полный контроль параметров
python projects/auth_management/scripts/authorize_users_from_csv.py "users.csv" --headless --relogin
```
**Назначение:** Базовый скрипт с настройкой всех параметров

### ⚡ Высокопроизводительное решение (500+ пользователей)
```bash
# Параллельная обработка
python projects/auth_management/scripts/parallel_auth_optimized.py "users.csv" --threads 5 --headless
```
**Назначение:** Оптимизированная параллельная авторизация больших объемов

### 🧪 Интеграция с фреймворком (отладка)
```bash
# GUI режим через pytest
python scripts/maintenance/run_csv_auth_gui.py
```
**Назначение:** Демонстрация и отладка через тестовый фреймворк

### 📊 Проверка результатов
```bash
# Валидация сохраненных куков
python projects/auth_management/run_mass_cookie_test.py --headless --parallel
```

**📖 Подробное руководство:** `projects/auth_management/BULK_AUTH_README.md`

## 🚀 Быстрый старт: Запуск эталонного теста авторизации

Тест `test_single_user_login_and_save_cookie.py` проверяет авторизацию через UI для одного или всех пользователей из `creds.env`.

**Примеры запуска:**

- **Все пользователи, headless (по умолчанию, без GUI):**
  ```
  pytest tests/integration/test_single_user_login_and_save_cookie.py -v -s
  ```

- **Все пользователи, с GUI (headed):**
  ```
  pytest tests/integration/test_single_user_login_and_save_cookie.py --headed -v -s
  ```

- **Только admin, headless:**
  ```
  AUTH_MODE=one pytest tests/integration/test_single_user_login_and_save_cookie.py -v -s
  ```

- **Только admin, с GUI (headed):**
  ```
  AUTH_MODE=one pytest tests/integration/test_single_user_login_and_save_cookie.py --headed -v -s
  ```

> Все параметры берутся из `creds.env`.  
> Подробнее — см. docstring внутри самого теста.

## 🔧 Описание компонентов

### 📦 Framework (framework/)
**Основа фреймворка автоматизации**
- `utils/` - Переиспользуемые утилиты (авторизация, отчетность, безопасность)
- `fixtures/` - Общие фикстуры pytest для всех тестов
- `app/` - Специфичная логика приложения

**Ключевые модули:**
- `auth_utils.py` - Авторизация и работа с пользователями
- `allure_utils.py` - Интеграция с Allure для красивых отчетов
- `secure_auth_utils.py` - Безопасная работа с учетными данными
- `cookie_constants.py` - Константы для работы с cookies

### 🧪 Tests (tests/)
**Тестовые сценарии**
- `e2e/` - End-to-End тесты (полные пользовательские сценарии)
- `integration/` - Интеграционные тесты (взаимодействие компонентов)

### 🎯 Projects (projects/)
**Подпроекты и специфичная логика**
- `auth_management/` - Проект управления авторизацией

### ⚙️ Config (config/)
**Конфигурационные файлы**
- `pytest/` - Настройки pytest (pytest.ini)
- `allure/` - Конфигурация отчетов (allure.properties, categories.json)
- `browsers/` - Настройки браузеров (browser_config.json)

### 🔐 Безопасность
- `secrets/` - Секретные данные (переменные окружения, CSV с пользователями)
- `cookies/` - Файлы сессий (автоматически игнорируются git)

## 📋 Лучшие практики

### Рекомендации по лучшим практикам:
1. **Безопасность превыше всего**: Никогда не коммитьте пароли и секреты
2. **Читаемость кода**: Используйте понятные имена переменных и функций
3. **Документирование**: Всегда добавляйте docstrings к новым функциям
4. **Тестирование**: Один тест = одна проверка
5. **Структура**: Следуйте существующей архитектуре проекта

### Работа с секретами:
- Все секреты храним в `secrets/` директории
- Используем переменные окружения через `.env` файлы
- Никогда не хардкодим пароли в коде
- Проверяем `.gitignore` перед коммитом

### Расширение фреймворка:
1. Новые утилиты добавляем в `framework/utils/`
2. Общие фикстуры размещаем в `framework/fixtures/`
3. Специфичную логику проектов в `projects/`
4. Конфигурации в соответствующие папки `config/`

## 📚 Документация по папкам BLL_tests

- **docs/**: Центральная документация (tests_overview.md, glossary.md и т.д.)
- **framework/**: Описание в ARCHITECTURE.md
- **tests/**: Руководство в TESTING_GUIDE.md
- **projects/auth_management/**: Локальный README.md с примерами скриптов
- **scripts/**: README.md с обзором утилит
Подробнее — смотрите docs/auto/ для Sphinx.

---

_Если вы впервые — начните с чтения README в каждой ключевой папке!_
