# BLL Tests: Фреймворк автоматизации тестирования

## 🏗️ Структура проекта

```
Bll_tests/                              # Корневая директория
├── framework/                          # Общий фреймворк автоматизации
│   ├── utils/                         # Общие утилиты (auth_utils, allure_utils)
│   └── app/                           # Специфичный код приложения
├── tests/                             # Тестовые директории
│   ├── integration/                   # Интеграционные тесты (основная тестовая база)
│   │   ├── redirect_tests/            # Тесты редиректов (были перенесены из e2e)
│   │   ├── sso/                       # SSO-тесты (были перенесены из e2e)
│   │   └── infrastructure/            # Вспомогательные интеграционные тесты
│   └── e2e/                           # Инфраструктурные файлы, заглушки, настройки (тестов нет)
├── scripts/                           # Общие скрипты
├── docs/                              # Документация
├── config/                            # Конфигурация фреймворка
├── secrets/                           # Секретные данные (в .gitignore)
│   ├── .env                           # Переменные окружения
│   ├── bulk_users.csv                 # CSV с данными пользователей
│   ├── creds.env                      # Креды
│   └── test_users.csv                 # Тестовые пользователи
└── cookies/                           # Файлы cookies (в .gitignore)
    ├── admin_cookies.json
    ├── user_cookies.json
    ├── moderator_cookies.json
    └── [другие cookie файлы]
```

## 🚀 Быстрый старт

### ⚡ Запуск тестов (самое быстрое)
```bash
# Windows - двойной клик по батнику:
scripts/run_tests_quick.bat        # ~15 сек, краткий вывод
scripts/run_tests_parallel.bat     # ~20 сек, подробный вывод

# Командная строка:
python -m pytest -n 10 -v          # Параллельно, 10 потоков
```

📖 **Полный гид**: [`scripts/QUICK_TESTING_GUIDE.md`](scripts/QUICK_TESTING_GUIDE.md)

### 1. Установка зависимостей
```bash
# Установка зависимостей фреймворка
pip install -r requirements.txt

# Установка браузеров для тестирования
playwright install
```

### 2. Запуск тестов
```bash
# Интеграционные тесты (основная тестовая база)
pytest tests/integration/ -v --alluredir=allure-results

# Отчет Allure
allure serve allure-results
```

### 3. Настройка секретов
1. Скопируйте `secrets/.env.example` в `secrets/.env`
2. Заполните необходимые переменные окружения
3. Убедитесь, что файлы секретов не попадают в git (.gitignore)

## 🔐 Массовая авторизация пользователей

### Запуск массового тестера кук
```bash
# Массовая проверка всех cookie файлов
python scripts/maintenance/cookie_tester.py
```

### 📊 Проверка результатов
```bash
# Валидация сохраненных куков
python scripts/maintenance/run_csv_auth_gui.py
```

## 🚀 Запуск эталонного теста авторизации

Тест `test_single_user_creds_login_and_save_cookie.py` проверяет авторизацию через UI для всех пользователей из `creds.env`.

**Примеры запуска:**

- **Все пользователи, headless (по умолчанию, без GUI):**
  ```
  pytest tests/auth/test_single_user_creds_login_and_save_cookie.py -v -s
  ```

- **Все пользователи, с GUI (headed):**
  ```
  pytest tests/auth/test_single_user_creds_login_and_save_cookie.py --headed -v -s
  ```

> Все параметры берутся из `creds.env`. Подробнее — см. docstring внутри теста.

## 🔧 Описание компонентов

### 📦 Framework (framework/)
**Основа фреймворка автоматизации**
- `utils/` - Переиспользуемые утилиты (авторизация, отчетность, безопасность)
- `app/` - Специфичная логика приложения

**Ключевые модули:**
- `auth_utils.py` - Авторизация и работа с пользователями
- `allure_utils.py` - Интеграция с Allure для красивых отчетов
- `cookie_constants.py` - Константы для работы с cookies

### 🧪 Tests (tests/)
**Тестовые сценарии**
- `integration/` - Интеграционные тесты (основная тестовая база)
  - `redirect_tests/` — тесты редиректов
  - `sso/` — тесты SSO
  - `infrastructure/` — инфраструктурные интеграционные тесты
- `e2e/` - Инфраструктурные файлы, заглушки, настройки (тестов нет)

### ⚙️ Config (config/)
**Конфигурационные файлы**
- Настройки pytest, allure, и браузеров

### 🔐 Безопасность
- `secrets/` - Секретные данные (переменные окружения, CSV с пользователями)
- `cookies/` - Файлы сессий (автоматически игнорируются git)

## 📋 Лучшие практики

### Рекомендации по лучшим практикам:
1. **Безопасность превыше всего**: Никогда не коммитьте пароли и секреты
2. **Читаемость кода**: Используйте понятные имена переменных и функций
3. **Документирование**: Всегда добавляйте docstrings к новым функциям
4. **Тестирование**: Один тест = одна проверка
5. **Структура**: Следуйте существующей архитектуре проекта

В случае вопросов или неполадок, начните с чтения документации в каждой ключевой папке!
---
_Эта документация отражает реальную структуру проекта и может быть обновлена в будущем._

_Для доступной работы с проектом рекомендуется ознакомиться с README в каждой ключевой папке!_
