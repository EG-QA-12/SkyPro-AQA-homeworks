# 🔒 Система управления секретами

Комплексная система безопасной работы с авторизационными данными в автотестах.

## 📋 Описание

Эта система обеспечивает:
- ✅ Безопасное хранение паролей и токенов
- ✅ Автоматическую загрузку конфигурации
- ✅ Типизированный доступ к секретам
- ✅ Защиту от случайных коммитов
- ✅ Поддержку различных окружений
- ✅ Детальное логирование без раскрытия секретов

## 🚀 Быстрый старт

### 1. Автоматическая настройка

```bash
# Запустите мастер настройки
python setup_secrets.py
```

Мастер автоматически:
- Установит зависимости
- Создаст файлы конфигурации
- Проверит безопасность
- Предоставит инструкции

### 2. Ручная настройка

```bash
# Установите зависимости
pip install python-dotenv playwright

# Создайте .env файл из шаблона
cp config/.env.template config/.env

# Заполните реальными данными
notepad config/.env  # или любой другой редактор
```

### 3. Заполните конфигурацию

Отредактируйте `config/.env`:

```env
# ОБЯЗАТЕЛЬНЫЕ НАСТРОЙКИ
AUTH_USERNAME=your_test_username
AUTH_PASSWORD=your_test_password
AUTH_DOMAIN=test.example.com

# ОПЦИОНАЛЬНЫЕ НАСТРОЙКИ
TEST_ENVIRONMENT=dev
DEBUG_MODE=false
HEADLESS=true

# ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ
API_BASE_URL=https://api.test.example.com
BROWSER_TYPE=chromium
SCREENSHOT_ON_FAILURE=true
ALLURE_REPORTS=true
```

### 4. Проверьте настройку

```bash
# Проверка конфигурации
python config/secrets_manager.py

# Проверка auth утилит
python secure_auth_utils.py
```

## 📁 Структура файлов

```
config/
├── secrets_manager.py      # Основной менеджер секретов
├── .env.template          # Шаблон конфигурации
├── .env                   # Ваши секреты (НЕ коммитить!)
├── .gitignore             # Правила защиты
└── README.md              # Эта документация

secure_auth_utils.py       # Безопасные утилиты авторизации
setup_secrets.py          # Мастер настройки
```

## 🔧 Использование в тестах

### Обновление существующих тестов

**Было:**
```python
from auth_utils import save_cookie, load_cookie
```

**Стало:**
```python
from secure_auth_utils import save_cookie, load_cookie
```

### Получение конфигурации

```python
from config.secrets_manager import get_config

# Получить полную конфигурацию
config = get_config()

# Использовать авторизационные данные
print(f"Домен: {config.auth.domain}")
print(f"Режим отладки: {config.debug_mode}")

# API данные (если настроены)
if config.api:
    print(f"API URL: {config.api.base_url}")
```

### Работа с менеджером авторизации

```python
from secure_auth_utils import auth_manager

# Проверка наличия куки
if auth_manager.check_auth_cookie_exists(context):
    print("Пользователь авторизован")

# Получение значения куки
cookie_value = auth_manager.get_auth_cookie_value(context)

# Очистка авторизационных куки
auth_manager.clear_auth_cookies(context)
```

### Создание куки

```python
from secure_auth_utils import create_joint_cookie

# Создание куки для Playwright
cookie = create_joint_cookie(
    value="session_token_value",
    domain="test.example.com"
)

context.add_cookies([cookie])
```

## 🌍 Управление окружениями

### Файлы конфигурации по приоритету

1. `config/.env.local` (локальные настройки, высший приоритет)
2. `config/.env.test` (тестовое окружение)
3. `config/.env.dev` (разработка)
4. `config/.env` (основные настройки)

### Переменная окружения

```env
# В .env файле
TEST_ENVIRONMENT=dev        # dev, test, staging, prod
```

### Программное определение

```python
from config.secrets_manager import secrets_manager

env = secrets_manager.current_environment
print(f"Текущее окружение: {env.value}")
```

## 🔒 Безопасность

### ✅ Что делать

- Используйте только тестовые учетные данные
- Регулярно меняйте пароли тестовых аккаунтов
- Проверяйте `git status` перед коммитом
- Используйте переменные окружения в CI/CD

### ❌ Чего НЕ делать

- Не используйте production данные в тестах
- Не коммитьте файлы .env с реальными данными
- Не логируйте значения паролей и токенов
- Не передавайте секреты через URL или заголовки

### Проверка безопасности

```bash
# Убедитесь, что .env не отслеживается Git
git status

# Поиск секретов в истории коммитов
git log --oneline -p | grep -i "password\|secret\|token\|key"

# Проверка текущих изменений
git diff --staged
```

## 🐛 Диагностика проблем

### Проблема: Модуль не найден

```bash
# Убедитесь, что находитесь в корне проекта
cd D:\Bll_tests

# Проверьте PYTHONPATH
python -c "import sys; print('\n'.join(sys.path))"
```

### Проблема: Переменные окружения не загружаются

```bash
# Установите python-dotenv
pip install python-dotenv

# Проверьте наличие .env файла
ls config/.env

# Проверьте кодировку файла (должна быть UTF-8)
file config/.env
```

### Проблема: Ошибки авторизации

```python
# Проверьте конфигурацию
from config.secrets_manager import secrets_manager
summary = secrets_manager.get_masked_config_summary()
print(summary)

# Валидация обязательных настроек
from config.secrets_manager import validate_required_config
validate_required_config(['auth'])
```

## 📚 API Reference

### SecretsManager

```python
from config.secrets_manager import SecretsManager

manager = SecretsManager()

# Получение обязательной переменной
username = manager.get_required_env("AUTH_USERNAME")

# Получение опциональной переменной
api_key = manager.get_optional_env("API_KEY", "default_value")

# Получение boolean значения
debug = manager.get_bool_env("DEBUG_MODE", False)

# Получение числового значения
timeout = manager.get_int_env("TIMEOUT", 30)
```

### SecureAuthManager

```python
from secure_auth_utils import SecureAuthManager

auth = SecureAuthManager()

# Сохранение куки
success = auth.save_auth_cookie(context, "cookies.json")

# Загрузка куки
success = auth.load_auth_cookie(context, "cookies.json")

# Создание куки
cookie_data = auth.create_auth_cookie("token_value")

# Добавление куки в контекст
auth.add_auth_cookie_to_context(context, "token_value")
```

## 🔄 Миграция с auth_utils.py

### Автоматическая замена в тестах

```bash
# Поиск всех файлов с импортами auth_utils
grep -r "from auth_utils import" .

# Замена импортов (Linux/Mac)
find . -name "*.py" -exec sed -i 's/from auth_utils import/from secure_auth_utils import/g' {} +

# Замена импортов (Windows PowerShell)
Get-ChildItem -Recurse -Filter "*.py" | ForEach-Object {
    (Get-Content $_.FullName) -replace 'from auth_utils import', 'from secure_auth_utils import' | Set-Content $_.FullName
}
```

### Обновление функций

Все функции из `auth_utils.py` поддерживаются в `secure_auth_utils.py`:

- `save_cookie()` → работает без изменений
- `load_cookie()` → работает без изменений  
- `joint_cookie()` → переименована в `create_joint_cookie()`

## 📞 Поддержка

При возникновении проблем:

1. Запустите диагностику: `python setup_secrets.py`
2. Проверьте логи: `python config/secrets_manager.py`
3. Проверьте безопасность: `git status`

---

**Автор:** Lead SDET Architect  
**Дата создания:** 2025-06-27  
**Версия:** 1.0.0
