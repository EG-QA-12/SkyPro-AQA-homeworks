# ============================================================================
# КОНФИГУРАЦИОННЫЕ ФАЙЛЫ С СЕКРЕТАМИ - МАКСИМАЛЬНАЯ ЗАЩИТА
# ============================================================================

# КРИТИЧЕСКИ ВАЖНО: Эти файлы НИКОГДА не должны попасть в Git!
# Содержат авторизационные данные, пароли, API ключи

# Основные .env файлы
.env
.env.*
!.env.template

# Файлы с учетными данными
auth_data/
credentials/
passwords/
secrets/
*.credentials
*.secret
*.key

# Конфигурационные файлы с секретами
config.ini
config.json
config.yaml
config.yml
settings.json
local_settings.py

# Файлы с куками и токенами
*_cookies.json
*cookies*
*tokens*
session_*
auth_tokens*
bearer_*

# Файлы сертификатов и ключей
*.pem
*.crt
*.cer
*.p12
*.pfx
*.jks
*.keystore

# ============================================================================
# ВРЕМЕННЫЕ И СЛУЖЕБНЫЕ ФАЙЛЫ
# ============================================================================

# Python кэш
__pycache__/
*.py[cod]
*$py.class

# Логи
*.log
*.log.*

# Временные файлы
*.tmp
*.temp
*.bak
*.swp
*.swo

# ============================================================================
# ПРАВИЛА ИСКЛЮЧЕНИЙ (РАЗРЕШЕННЫЕ ФАЙЛЫ)
# ============================================================================

# Разрешаем шаблоны конфигурации (без реальных данных)
!.env.template
!config.template.ini
!secrets.template.json

# Разрешаем документацию по настройке
!README.md
!SETUP.md
!CONFIGURATION.md

# ============================================================================
# ДОПОЛНИТЕЛЬНАЯ ЗАЩИТА
# ============================================================================

# Файлы с любыми вариациями названий секретов
*secret*
*password*
*credential*
*auth*
*token*
*api_key*

# НО исключаем наши безопасные модули (они не содержат реальных секретов)
!secrets_manager.py
!*auth_utils.py

# ============================================================================
# НАПОМИНАНИЯ ДЛЯ РАЗРАБОТЧИКОВ
# ============================================================================

# ВАЖНО: Перед коммитом всегда проверяйте:
# 1. git status - нет ли файлов с секретами
# 2. git diff --staged - не добавили ли секреты в код
# 3. Используйте переменные окружения вместо хардкода

# КОМАНДЫ ДЛЯ ПРОВЕРКИ БЕЗОПАСНОСТИ:
# git log --oneline -p | grep -i "password\|secret\|token\|key" 
# git secrets --scan (если установлен git-secrets)

# ЕСЛИ СЛУЧАЙНО ЗАКОММИТИЛИ СЕКРЕТЫ:
# 1. Немедленно поменяйте все скомпрометированные пароли
# 2. Используйте git filter-branch или BFG Repo-Cleaner
# 3. Уведомите команду безопасности
