#!/usr/bin/env python3
"""
Скрипт запуска GUI приложения для управления авторизацией.

Использование:
    python run_gui.py

Требования:
    - Python 3.8+
    - Tkinter (обычно включен в стандартную поставку Python)
    - Playwright
    - Все зависимости из requirements.txt
"""

import sys
import os
from pathlib import Path

# Добавляем путь к корню проекта в sys.path
project_root = Path(__file__).resolve().parent
sys.path.insert(0, str(project_root))

try:
    # Проверяем наличие всех необходимых модулей
    from src.auth_gui import main
    
    print("🚀 Запуск GUI для управления авторизацией...")
    print("📁 Корневая директория проекта:", project_root)
    print("⚙️ Инициализация интерфейса...")
    
    # Запускаем GUI
    main()
    
except ImportError as e:
    print(f"❌ Ошибка импорта: {e}")
    print("\n🔧 Возможные решения:")
    print("1. Убедитесь, что установлены все зависимости: pip install -r requirements.txt")
    print("2. Проверьте, что Playwright установлен: python -m playwright install")
    print("3. Убедитесь, что файлы проекта находятся в правильной структуре")
    sys.exit(1)
    
except Exception as e:
    print(f"❌ Неожиданная ошибка: {e}")
    print("🔍 Проверьте логи для получения дополнительной информации")
    sys.exit(1)
