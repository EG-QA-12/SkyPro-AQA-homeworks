# Auth Project - Система автоматизации тестирования с управлением пользователями

🚀 Комплексная система для автоматизации тестирования веб-приложений с расширенным управлением пользователями, сессиями и авторизацией через куки.

## 🎯 Основные возможности

- **🖥️ Графический интерфейс (GUI)**: Удобное управление авторизацией через GUI приложение
- **Управление пользователями**: SQLite БД с bcrypt-шифрованием паролей
- **Авторизация через Playwright**: Автоматическая авторизация и сохранение сессий
- **Система ролей**: Поддержка различных ролей (admin, moderator, user, qa, tester, expert)
- **Управление куками**: Автоматическое сохранение и валидация сессий
- **Массовый импорт**: Добавление пользователей из CSV файлов
- **CLI инструменты**: Удобные скрипты для управления пользователями
- **Page Object Model**: Структурированные тесты с использованием POM
- **Логирование**: Подробное логирование всех операций

## 🚀 Быстрый старт

### 🖥️ Запуск через GUI (Рекомендуется)

```bash
# 1. Установка зависимостей
pip install -r requirements.txt

# 2. Установка браузеров Playwright
playwright install

# 3. Настройка конфигурации
cp data/creds.env.example data/creds.env
# Отредактируйте data/creds.env и укажите LOGIN и PASS

# 4. Инициализация пользователей (если есть скрипт)
python scripts/init_users.py

# 5. Запуск GUI приложения
python run_gui.py
```
Имя и описание проекта: Authorisation Management

### 💻 Запуск через командную строку

```bash
# Выполните шаги 1-4 из GUI варианта, затем:

# 5. Запуск тестов
pytest tests/ -v
```

## 📋 Требования

- **Python**: 3.9+
- **Операционная система**: Windows, macOS, Linux
- **Зависимости**: См. `requirements.txt`
  - playwright (автоматизация браузера)
  - pytest + pytest-playwright (тестирование)
  - bcrypt (безопасность паролей)
  - pydantic-settings (конфигурация)
  - python-dotenv (переменные окружения)
  - SQLite3 (встроен в Python)

## 📁 Структура проекта

```
auth_project/
├── src/                           # Основная бизнес-логика
│   ├── __init__.py
│   ├── auth.py                    # Авторизация и работа с куками
│   ├── auth_manager.py            # CLI управление авторизацией
│   ├── cli.py                     # Аргументы командной строки
│   ├── config.py                  # Конфигурация проекта
│   ├── cookies.py                 # Утилиты для работы с куками
│   ├── database.py                # Управление SQLite БД
│   ├── logger.py                  # Настройка логирования
│   └── user_manager.py            # Управление пользователями
├── data/                          # Данные проекта (не в git)
│   ├── creds.env.example          # Пример файла конфигурации
│   ├── *_cookies.json             # Файлы куков пользователей
│   └── users.db                   # База данных пользователей
├── scripts/                       # CLI скрипты
│   ├── auth_integration.py        # Интеграция авторизации
│   ├── authorize_users_from_csv.py # Массовая авторизация из CSV
│   ├── bulk_add_users.py          # Массовое добавление пользователей
│   ├── clean_db.py                # Очистка базы данных
│   ├── delete_users.py            # Удаление пользователей
│   ├── init_users.py              # Инициализация тестовых пользователей
│   └── view_users.py              # Просмотр пользователей
├── tests/                         # Автотесты
│   ├── __init__.py
│   ├── conftest.py                # Фикстуры pytest
│   ├── test_auth_debug.py         # Тесты отладки авторизации
│   └── test_example.py            # Примеры тестов
├── pages/                         # Page Object Model
│   └── login_page.py              # Страница логина
├── config.py                      # Глобальная конфигурация
├── example_users.csv              # Пример CSV с пользователями
├── test_users.csv                 # Тестовые пользователи
├── requirements.txt               # Python зависимости
├── .flake8                        # Настройки линтера
├── .isort.cfg                     # Настройки сортировки импортов
├── .pre-commit-config.yaml        # Pre-commit хуки
├── .gitignore                     # Git исключения
└── README.md                      # Документация
```

## ⚙️ Конфигурация

### Настройка переменных окружения

Скопируйте файл примера конфигурации и заполните его:

```bash
cp data/creds.env.example data/creds.env
```

Пример содержимого `data/creds.env`:

```env
# Основные учетные данные для тестирования
LOGIN=test_user
PASS=test_password

# URL адреса тестируемого приложения
BASE_URL=https://ca.bll.by
LOGIN_URL=https://ca.bll.by/login
TARGET_URL=https://ca.bll.by/

# Настройки браузера
HEADLESS=true

# Логирование
LOG_LEVEL=INFO
```

## 🖥️ Графический интерфейс (GUI)

### Описание GUI приложения

GUI приложение предоставляет удобный графический интерфейс для управления авторизацией пользователей без необходимости работы с командной строкой.

### Возможности GUI

#### 🔐 Вкладка "Авторизация"
- **Выбор пользователя**: Dropdown список всех пользователей с указанием их ролей
- **Информация о пользователе**: Отображение деталей выбранного пользователя и статуса куков
- **Авторизация**: Запуск браузера для выполнения авторизации (с визуальным режимом или headless)
- **Проверка куков**: Быстрая валидация сохранённых куков без запуска браузера
- **Тестовая авторизация**: Проверка доступа к целевому URL с использованием сохранённых куков
- **Очистка куков**: Удаление куков конкретного пользователя
- **Headless режим**: Переключатель для запуска браузера в фоновом режиме
- **Лог операций**: Область с цветным отображением результатов операций

#### 👥 Вкладка "Пользователи"
- **Таблица пользователей**: Список всех пользователей с информацией о роли, статусе куков и последней авторизации
- **Обновление списка**: Автоматическое обновление статуса всех пользователей
- **Массовая очистка**: Удаление всех сохранённых куков одним нажатием

#### 📋 Вкладка "Логи"
- **Просмотр логов**: Отображение последних 1000 строк из лог-файла
- **Обновление логов**: Обновление содержимого в реальном времени
- **Очистка логов**: Удаление всех логов

### Запуск GUI

```bash
# Быстрый запуск
python run_gui.py

# Или через модуль
python -m src.auth_gui
```

### Интерфейс GUI

```
┌─────────────────────────────────────────────────────────────────┐
│ Auth Project - GUI Manager                                      │
├─────────────────────────────────────────────────────────────────┤
│ Файл | Помощь                                                   │
├─────────────────────────────────────────────────────────────────┤
│ [Авторизация] [Пользователи] [Логи]                             │
├─────────────────────────────────────────────────────────────────┤
│ ┌─ Выбор пользователя ────────────────────────────────────────┐ │
│ │ Пользователь: [admin (admin)           ▼]                   │ │
│ │ ┌─────────────────────────────────────────────────────────┐ │ │
│ │ │ Логин: admin                                            │ │ │
│ │ │ Роль: admin                                             │ │ │
│ │ │ Email: admin@example.com                                │ │ │
│ │ │ ID: 000001                                              │ │ │
│ │ │ Статус куков: ✅ Валидны                                │ │ │
│ │ └─────────────────────────────────────────────────────────┘ │ │
│ └─────────────────────────────────────────────────────────────┘ │
│ ┌─ Действия ──────────────────────────────────────────────────┐ │
│ │ [🔐 Авторизоваться] [🍪 Проверить куки] [🧪 Тестовая авториз] │ │
│ │ ☐ Headless режим                        [🗑️ Очистить куки] │ │
│ └─────────────────────────────────────────────────────────────┘ │
│ ┌─ Статус ────────────────────────────────────────────────────┐ │
│ │ [████████████████████████████████████████] (Прогресс-бар)   │ │
│ │ ┌───────────────────────────────────────────────────────────┐ │ │
│ │ │ [08:15:32] ✅ Авторизация пользователя admin завершена   │ │ │
│ │ │ [08:16:14] ℹ️ Проверка куков для пользователя admin      │ │ │
│ │ │ [08:16:15] ✅ Куки пользователя admin валидны            │ │ │
│ │ └───────────────────────────────────────────────────────────┘ │ │
│ └─────────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────────┤
│ Готов к работе                                               ● │
└─────────────────────────────────────────────────────────────────┘
```

### Возможные сценарии использования

1. **Быстрая авторизация для тестирования**:
   - Выберите нужного пользователя
   - Нажмите "Авторизоваться"
   - Куки сохранятся автоматически

2. **Проверка статуса авторизации**:
   - Используйте "Проверить куки" для быстрой валидации
   - "Тестовая авторизация" для полной проверки доступа

3. **Управление несколькими пользователями**:
   - Перейдите на вкладку "Пользователи"
   - Просмотрите статус всех пользователей
   - Очистите куки при необходимости

4. **Отладка проблем**:
   - Используйте вкладку "Логи" для диагностики
   - Включите подробное логирование в настройках

### Преимущества GUI

- ✅ **Простота использования**: Не требует знания командной строки
- ✅ **Визуальная обратная связь**: Цветное отображение результатов операций
- ✅ **Мультизадачность**: Операции выполняются в отдельных потоках
- ✅ **Безопасность**: Пароли не отображаются в интерфейсе
- ✅ **Производительность**: Быстрая проверка куков без запуска браузера
- ✅ **Универсальность**: Работает на Windows, macOS, Linux

## 🗄️ Работа с базой данных пользователей

### Управление пользователями через DatabaseManager

```python
from src.database import DatabaseManager

db = DatabaseManager()

# Создание нового пользователя (роль "user", подписка уровня 1)
db.create_user("user1", "password123")

# Создание пользователя со средним уровнем подписки (2)
db.create_user("user2", "password456", subscription=2)

# Создание администратора (автоматически получит подписку уровня 3)
db.create_user("admin", "adminpass", role="admin")

# Создание модератора (автоматически получит подписку уровня 3)
db.create_user("mod", "modpass", role="moderator")

# Обновление пароля существующего пользователя
db.create_user("user1", "newpassword")

# Проверка пароля
if db.verify_password("user1", "newpassword"):
    print("Пароль верный")
    
# Получение данных пользователя
user = db.get_user_by_username("admin")
print(f"Роль: {user['role']}, Подписка: {user['subscription']}")

# Удаление пользователя
db.delete_user("user2")
```

### Управление пользователями через UserManager

```python
from src.user_manager import UserManager

user_manager = UserManager()

# Добавление пользователя с дополнительными полями
user_manager.add_user(
    login="john_doe",
    password="securepass123",
    role="qa",
    email="john@example.com",
    phone="+1234567890"
)

# Получение пользователя по логину
user = user_manager.get_user(login="john_doe")

# Получение пользователя по роли
admin_user = user_manager.get_user_by_role("admin")

# Получение всех пользователей
all_users = user_manager.get_all_users()

# Проверка пароля
if user_manager.verify_password("john_doe", "securepass123"):
    print("Аутентификация прошла успешно")
```

### Работа с куками

```python
from src.auth import load_cookies, save_cookies
from src.user_manager import UserManager
import json

user_manager = UserManager()

# Авторизация пользователя по роли и получение кук
cookies_data = user_manager.authorize_by_role("admin", headless=True)
if cookies_data:
    print(f"Получены куки, действительны до: {cookies_data['expiration']}")

# Авторизация пользователя по логину/паролю
cookies_data = user_manager.authorize_by_credentials(
    "test_user", "test_password", role="qa"
)

# Загрузка кук из файла
cookies = load_cookies("data/admin_cookies.json")
if cookies:
    print(f"Загружено {len(cookies)} куков")
```

## 🛠️ CLI Скрипты

### Основные скрипты управления

| Скрипт | Описание | Команда |
|--------|----------|----------|
| `scripts/bulk_add_users.py` | Массовое добавление пользователей в БД | `python scripts/bulk_add_users.py --csv users.csv` |
| `scripts/authorize_users_from_csv.py` | Массовая авторизация пользователей из CSV | `python scripts/authorize_users_from_csv.py users.csv` |
| `scripts/view_users.py` | Просмотр всех пользователей в БД | `python scripts/view_users.py` |
| `scripts/clean_db.py` | Очистка базы данных | `python scripts/clean_db.py` |
| `scripts/delete_users.py` | Удаление пользователей | `python scripts/delete_users.py --username user1` |
| `scripts/init_users.py` | Инициализация тестовых пользователей | `python scripts/init_users.py` |

### Примеры использования UserManager

```python
from src.user_manager import UserManager

user_manager = UserManager()

# Авторизация пользователя по роли
cookies_data = user_manager.authorize_by_role("admin", headless=True)
if cookies_data:
    print(f"Получены куки для админа, истекают: {cookies_data['expiration']}")

# Массовая авторизация из CSV
result = user_manager.authorize_users_from_csv("example_users.csv")
print(f"Успешно: {len(result['success'])}, Ошибки: {len(result['failed'])}")
```

## 📊 Формат CSV файлов

Для массового импорта пользователей используйте следующий формат:

```csv
username,password,role,subscription
ivan,pass123,user,1
admin,adminpass,admin,3
user03,TestPass03,moderator,3
user04,TestPass04,tester,2
user05,TestPass05,user,1
user06,TestPass06,qa,2
```

**Поля:**
- `username` - имя пользователя для входа
- `password` - пароль
- `role` - роль пользователя (admin, moderator, user, qa, tester, expert)
- `subscription` - уровень подписки (1 - малое, 2 - среднее, 3 - крупное предприятие)

> 💡 **Примечание**: для ролей `admin` и `moderator` автоматически устанавливается максимальный уровень подписки (3)

## 🧪 Запуск тестов

```bash
# Запуск всех тестов
pytest tests/ -v

# Запуск конкретного теста
pytest tests/test_auth_debug.py -v

# Запуск с отчетом Allure (если настроен)
pytest tests/ --alluredir=allure-results
```

## 🛡️ Безопасность

- **Хеширование паролей**: Все пароли хешируются с использованием `bcrypt`
- **Конфиденциальные данные**: Файлы `data/creds.env` и `data/*.json` не коммитятся в git
- **Валидация**: Автоматическая проверка истечения сессий
- **Логирование**: Подробные логи без раскрытия чувствительных данных

## 🔧 Разработка и отладка

### Полезные команды

```bash
# Генерация тестов с Playwright Codegen
playwright codegen https://ca.bll.by

# Установка pre-commit хуков
pre-commit install

# Проверка кода линтерами
flake8 src/ tests/
isort --check-only src/ tests/
```

### Структура логирования

- Логи сохраняются в директории, указанной в конфигурации
- Уровень логирования настраивается через переменную `LOG_LEVEL`
- Поддерживаются уровни: DEBUG, INFO, WARNING, ERROR, CRITICAL

## 🤝 Вклад в проект

1. Создайте форк репозитория
2. Создайте ветку для новой функции (`git checkout -b feature/new-feature`)
3. Внесите изменения и добавьте тесты
4. Запустите линтеры и тесты
5. Создайте Pull Request

## 📞 Поддержка

- **Создание Issues**: Для багов и предложений используйте GitHub Issues
- **Документация**: Все ключевые функции имеют подробные docstrings
- **Примеры**: См. директорию `tests/` для примеров использования

## Интеграционные и bulk-скрипты

- Все массовые, интеграционные и вспомогательные скрипты для управления пользователями, авторизации, работы с БД и cookie находятся в папке scripts/.
- Каждый скрипт снабжён docstrings, поддерживает --help и описан в scripts/README.md.
- Для CI/CD и диагностики используйте только поддерживаемые скрипты, описанные в документации.
- Устаревшие тесты и Page Object-ы перенесены в основную структуру проекта (framework/tests) и здесь больше не поддерживаются.

---

🚀 **Готов к использованию!** Следуйте инструкциям быстрого старта для начала работы с проектом.
