# Визуальная массовая авторизация

## Варианты запуска с видимым GUI:

### 1. Тестовый режим (3 пользователя)
```bash
test_visual_auth.bat
```
Или прямая команда:
```bash
python scripts\authorize_users_from_csv.py "data\visual_test.csv" --relogin
```

### 2. Полная авторизация всех пользователей
```bash
run_bulk_auth_visual.bat
```
Или прямая команда:
```bash
python scripts\authorize_users_from_csv.py "data\bulk_users.csv" --relogin
```

### 3. Прямые команды Python (без интерактивного интерфейса):

**Тест с GUI:**
```bash
python scripts\authorize_users_from_csv.py "data\visual_test.csv" --relogin
```

**Полная авторизация с GUI:**
```bash
python scripts\authorize_users_from_csv.py "data\bulk_users.csv" --relogin
```

**Полная авторизация БЕЗ GUI (headless):**
```bash
python scripts\authorize_users_from_csv.py "data\bulk_users.csv" --headless --relogin
```

## Важные замечания:

⚠️ **НЕ закрывайте окна браузера вручную** во время процесса авторизации!

⚠️ **Визуальный режим** потребляет больше ресурсов системы.

⚠️ **Полная авторизация** займет 2-3 часа для всех пользователей.

💡 **Рекомендация:** Сначала протестируйте на 3 пользователях с помощью `test_visual_auth.bat`
