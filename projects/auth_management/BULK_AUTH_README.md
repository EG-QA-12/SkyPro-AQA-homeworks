# 🚀 Массовая авторизация пользователей - Инструкция

Данные скрипты упрощают запуск массовой авторизации для файла `D:\Bll_tests\auth_project\data\bulk_users.csv` без написания длинных команд.

## 📋 Доступные скрипты

### 🔹 BAT файлы (Windows Command Prompt)

| Файл | Описание | Режим |
|------|----------|-------|
| `run_bulk_auth.bat` | Обычная авторизация с отображением браузера | Визуальный |
| `run_bulk_auth_headless.bat` | Авторизация в скрытом режиме | Скрытый |
| `run_bulk_auth_force.bat` | Принудительная переавторизация всех | Скрытый + Force |

### 🔹 PowerShell скрипт

| Файл | Описание |
|------|----------|
| `run_bulk_auth.ps1` | Универсальный PowerShell скрипт с параметрами |

### 🔹 Python скрипт

| Файл | Описание |
|------|----------|
| `run_bulk_auth.py` | Универсальный Python скрипт с аргументами |

## 🎯 Быстрый запуск

### Самый простой способ (Windows):
1. **Двойной клик** по файлу `run_bulk_auth.bat`
2. Подтвердите запуск нажатием любой клавиши
3. Ждите завершения авторизации

### Для скрытого режима:
1. **Двойной клик** по файлу `run_bulk_auth_headless.bat`
2. Авторизация пройдет без отображения браузера

## 📖 Подробное использование

### 🔸 BAT файлы

```batch
# Визуальный режим - браузер будет виден
run_bulk_auth.bat

# Скрытый режим - браузер не отображается
run_bulk_auth_headless.bat

# Принудительная переавторизация всех пользователей
run_bulk_auth_force.bat
```

### 🔸 PowerShell скрипт

```powershell
# Справка
.\run_bulk_auth.ps1 -Help

# Визуальный режим
.\run_bulk_auth.ps1

# Скрытый режим
.\run_bulk_auth.ps1 -Headless

# Принудительная переавторизация
.\run_bulk_auth.ps1 -Force

# Скрытый + принудительный режим
.\run_bulk_auth.ps1 -Headless -Force

# Использование другого CSV файла
.\run_bulk_auth.ps1 -CsvPath "path\to\another.csv"
```

### 🔸 Python скрипт

```bash
# Справка
python run_bulk_auth.py --help

# Визуальный режим
python run_bulk_auth.py

# Скрытый режим
python run_bulk_auth.py --headless

# Принудительная переавторизация
python run_bulk_auth.py --force

# Скрытый + принудительный режим
python run_bulk_auth.py --headless --force

# Использование другого CSV файла
python run_bulk_auth.py --csv-path "path/to/another.csv"
```

## ⚙️ Режимы работы

### 🔸 Визуальный режим (по умолчанию)
- Браузер отображается на экране
- Можно наблюдать процесс авторизации
- Полезно для отладки

### 🔸 Скрытый режим (headless)
- Браузер работает в фоне
- Быстрее выполняется
- Не мешает другой работе

### 🔸 Умная авторизация (по умолчанию)
- Пропускает пользователей с действующими куками
- Экономит время

### 🔸 Принудительная авторизация (force)
- Переавторизует ВСЕХ пользователей
- Игнорирует существующие куки
- Требует подтверждения

## 📊 Пример вывода

```
============================================================
  МАССОВАЯ АВТОРИЗАЦИЯ ПОЛЬЗОВАТЕЛЕЙ
============================================================
Файл CSV: D:\Bll_tests\auth_project\data\bulk_users.csv
Режим: СКРЫТЫЙ режим, пропуск пользователей с действующими куками
============================================================

🚀 Запуск авторизации...
   Браузер: скрытый
   Переавторизация: умная

[INFO] CSV обработка завершена. Успешно: 535, Ошибок: 0

============================================================
  РЕЗУЛЬТАТЫ АВТОРИЗАЦИИ
============================================================
✅ Успешно авторизовано: 535
❌ Ошибок: 0
============================================================
```

## 🛠️ Устранение неполадок

### ❌ Файл CSV не найден
```
ОШИБКА: Файл data\bulk_users.csv не найден!
```
**Решение**: Убедитесь, что файл `data\bulk_users.csv` существует в проекте.

### ❌ Python не найден
```
'python' не является внутренней или внешней командой
```
**Решение**: Установите Python и добавьте его в PATH, или используйте `python3`.

### ❌ Модули не найдены
```
ModuleNotFoundError: No module named 'src'
```
**Решение**: Запускайте скрипты из корневой директории проекта.

### ❌ Ошибки авторизации
- Проверьте настройки в `data/creds.env`
- Убедитесь, что сайт доступен
- Попробуйте визуальный режим для отладки

## 📝 Заметки

- **Время выполнения**: Зависит от количества пользователей (обычно 2-3 секунды на пользователя)
- **Ресурсы**: В скрытом режиме потребляет меньше CPU и памяти
- **Безопасность**: Пароли не отображаются в логах
- **Совместимость**: Работает на Windows, Linux, macOS

## 🔗 Связанные файлы

- `data/bulk_users.csv` - CSV файл с пользователями
- `scripts/authorize_users_from_csv.py` - Базовый скрипт авторизации
- `src/user_manager.py` - Логика управления пользователями
- `data/creds.env` - Конфигурация проекта

---

💡 **Совет**: Для ежедневного использования рекомендуется `run_bulk_auth_headless.bat` - быстро и без отвлекающих окон браузера.
