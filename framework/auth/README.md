# 🛡️ Система авторизации

## Обзор

Единая система управления авторизацией для фреймворка автотестов, объединяющая функциональность нескольких предыдущих реализаций.

## Архитектура

```
auth/
├── __init__.py              # Точка входа модуля
├── auth_manager.py          # Основной менеджер авторизации
├── cookie_provider.py       # Провайдер кук
├── api_auth.py             # API авторизация
├── auth_utils.py           # Вспомогательные утилиты
└── README.md               # Эта документация
```

## Основные компоненты

### AuthManager
Основной класс для управления авторизацией с поддержкой:
- Кэширования кук
- Автоматического API-логина
- Валидации сессий
- Retry логики

### CookieProvider
Провайдер кук с поддержкой различных источников:
- Переменные окружения
- Локальные файлы
- API-логин

### APIAuth
Модуль для API авторизации с поддержкой:
- Параллельной обработки
- Обработки ошибок
- Логирования

## Источники авторизации (в порядке приоритета)

1. **Переменные окружения**: `SESSION_COOKIE_{ROLE}` или `SESSION_COOKIE`
2. **Локальные файлы**: `cookies/{role}_session.txt` или `cookies/{role}_cookies.json`
3. **API-логин**: автоматический логин через учетные данные

## Обратная совместимость

Все старые интерфейсы сохранены для совместимости с существующими тестами.
