# Руководство по Chrome DevTools MCP Remote Mode

## 🎯 Обзор

Chrome DevTools MCP Remote Mode позволяет подключаться к уже запущенному браузеру Chrome и управлять им, открывая новые вкладки в существующей сессии пользователя.

## ✅ Возможности Remote Mode

- **Подключение к существующему браузеру**: Не запускает новый экземпляр Chrome
- **Работа с авторизованной сессией**: Использует куки и сессию пользователя
- **Открытие новых вкладок**: Создает вкладки в вашем браузере
- **Запись действий**: Отслеживает пользовательские действия для генерации тестов

## 🚀 Быстрый старт

### 1. Запуск Chrome с remote debugging

```bash
# Используйте подготовленный скрипт
scripts/start_chrome_remote_debug.bat
```

Или вручную:
```cmd
"C:\Program Files\Google\Chrome\Application\chrome.exe" --remote-debugging-port=9222 --user-data-dir="C:\temp\chrome-remote-debug"
```

### 2. Авторизация

В открывшемся браузере:
1. Авторизуйтесь на нужных сайтах (bll.by, expert.bll.by)
2. Перейдите на страницы, где будете тестировать

### 3. Использование в KiloKode/Cline

```javascript
// Используйте chrome-devtools-remote сервер
use_mcp_tool({
  server_name: "chrome-devtools-remote",
  tool_name: "list_pages",
  arguments: {}
});

// Открытие новой вкладки
use_mcp_tool({
  server_name: "chrome-devtools-remote",
  tool_name: "navigate_page",
  arguments: {"url": "https://expert.bll.by/questions/create"}
});
```

## 📋 Два режима работы

### 🔄 Режим 1: Новый браузер (`chrome-devtools`)
```json
{
  "chrome-devtools": {
    "command": "npx",
    "args": ["chrome-devtools-mcp@latest", "--executablePath=...", "--isolated"]
  }
}
```
- ✅ Запускает новый экземпляр Chrome
- ✅ Полная изоляция сессий
- ✅ Можно запускать несколько экземпляров

### 🔗 Режим 2: Существующий браузер (`chrome-devtools-remote`)
```json
{
  "chrome-devtools-remote": {
    "command": "npx",
    "args": ["chrome-devtools-mcp@latest", "--browserUrl=http://127.0.0.1:9222"]
  }
}
```
- ✅ Подключается к вашему браузеру
- ✅ Использует вашу авторизацию
- ✅ Открывает вкладки в существующей сессии

## 🎬 Сценарий создания вопроса (Remote Mode)

### Шаг 1: Подготовка браузера
```bash
scripts/start_chrome_remote_debug.bat
# Авторизуйтесь на bll.by вручную
```

### Шаг 2: Работа через MCP
```javascript
// 1. Проверить подключение
use_mcp_tool({
  server_name: "chrome-devtools-remote",
  tool_name: "list_pages",
  arguments: {}
});

// 2. Открыть страницу создания вопроса
use_mcp_tool({
  server_name: "chrome-devtools-remote",
  tool_name: "navigate_page",
  arguments: {"url": "https://expert.bll.by/questions/create"}
});

// 3. Сделать снимок страницы для анализа элементов
use_mcp_tool({
  server_name: "chrome-devtools-remote",
  tool_name: "take_snapshot",
  arguments: {}
});

// 4. Заполнить форму (используя uid из snapshot)
use_mcp_tool({
  server_name: "chrome-devtools-remote",
  tool_name: "fill",
  arguments: {"uid": "title_input_uid", "value": "Тестовый вопрос"}
});

use_mcp_tool({
  server_name: "chrome-devtools-remote",
  tool_name: "fill",
  arguments: {"uid": "content_textarea_uid", "value": "Описание вопроса"}
});

// 5. Отправить форму
use_mcp_tool({
  server_name: "chrome-devtools-remote",
  tool_name: "click",
  arguments: {"uid": "submit_button_uid"}
});
```

### Шаг 3: Генерация автотеста
```bash
# Используйте скрипт генерации
python scripts/record_question_creation.py
```

## 🔧 Конфигурация MCP серверов

### KiloKode (.kilocode/mcp.json)
```json
{
  "chrome-devtools": {
    "command": "npx",
    "args": ["chrome-devtools-mcp@latest", "--executablePath=C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe", "--isolated"],
    "env": {"CHROME_PATH": "C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe"}
  },
  "chrome-devtools-remote": {
    "command": "npx",
    "args": ["chrome-devtools-mcp@latest", "--browserUrl=http://127.0.0.1:9222"],
    "env": {"CHROME_PATH": "C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe"}
  }
}
```

### Cline (cline_mcp_settings.json)
```json
{
  "chrome-devtools": {
    "command": "npx",
    "args": ["chrome-devtools-mcp@latest", "--executablePath=C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe", "--isolated"],
    "autoApprove": ["take_snapshot", "list_pages"]
  },
  "chrome-devtools-remote": {
    "command": "npx",
    "args": ["chrome-devtools-mcp@latest", "--browserUrl=http://127.0.0.1:9222"],
    "autoApprove": ["take_snapshot", "list_pages", "navigate_page"]
  }
}
```

## 📁 Структура файлов

```
scripts/
├── start_chrome_remote_debug.bat    # Скрипт запуска Chrome с remote debugging
├── record_question_creation.py      # Генератор кода автотестов
└── ...

docs/
├── CHROME_DEVTOOLS_MCP_GUIDE.md     # Основное руководство
└── CHROME_DEVTOOLS_REMOTE_GUIDE.md  # Remote mode (этот файл)
```

## 🎯 Когда использовать Remote Mode

### ✅ Рекомендуется:
- Когда нужна авторизация пользователя
- Для тестирования в реальной среде
- Когда важно сохранить сессию и куки
- Для ручной подготовки тестового состояния

### ❌ Не рекомендуется:
- Для полностью автоматизированных тестов
- Когда нужна изоляция тестов
- Для параллельного запуска тестов

## 🚨 Важные замечания

### Авторизация
- **Всегда авторизуйтесь вручную** перед началом работы
- Remote mode использует вашу сессию браузера

### Порты
- Remote debugging использует порт **9222**
- Убедитесь, что порт свободен

### Безопасность
- Не записывайте реальные пароли в автотесты
- Используйте тестовые данные для форм

### Производительность
- Remote mode может быть медленнее из-за сетевых задержек
- Используйте для интерактивной работы, а не для CI/CD

## 🎉 Результат

При использовании Remote Mode вы получаете:
- ✅ **Доступ к авторизованной сессии**
- ✅ **Новые вкладки в вашем браузере**
- ✅ **Реальные условия тестирования**
- ✅ **Простую генерацию автотестов**

**Chrome DevTools MCP Remote Mode = Ваш браузер + AI-автоматизация! 🚀**