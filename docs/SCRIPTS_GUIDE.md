# 📚 Руководство по скриптам проекта BLL_tests

## 📣 ВАЖНОЕ ОБНОВЛЕНИЕ

**Новый унифицированный фреймворк** 🚀 - теперь доступен для упрощения написания тестов!
Смотрите подробности в [NEW_FRAMEWORK_GUIDE.md](NEW_FRAMEWORK_GUIDE.md) и [framework/README.md](../framework/README.md)

## 🎯 Обзор

Этот документ описывает все доступные скрипты в проекте и их назначение. Скрипты находятся в директории `scripts/maintenance/` и предназначены для упрощения рутинных задач тестирования.

## 🛠️ Доступные скрипты

### 1. cookie_tester.py
**Назначение:** Массовая проверка валидности сохраненных cookie файлов

**Использование:**
```bash
python scripts/maintenance/cookie_tester.py
```

**Функционал:**
- Сканирует папку `cookies/` на наличие JSON файлов
- Извлекает куку `test_joint_session` из каждого файла
- Проверяет авторизацию на сайте ca.bll.by
- Выводит детальную статистику результатов

**Пример вывода:**
```
🚀 НАЧИНАЕМ МАССОВОЕ ТЕСТИРОВАНИЕ
📂 Файлов к обработке: 10
[1/10] 🔍 Обрабатываем: admin_cookies.json
  ✅ Кука найдена
  ✅ Авторизация успешна (код: 200)
```

### 2. run_csv_auth_gui.py
**Назначение:** GUI режим для массовой авторизации пользователей из CSV файла

**Использование:**
```bash
python scripts/maintenance/run_csv_auth_gui.py
```

**Функционал:**
- Читает пользователей из CSV файла в `secrets/`
- Запускает браузер в GUI режиме для визуальной отладки
- Авторизует каждого пользователя и сохраняет cookies
- Показывает прогресс в реальном времени

### 3. run_tests_with_allure.py
**Назначение:** Запуск тестов с автоматической генерацией Allure отчетов

**Использование:**
```bash
python scripts/maintenance/run_tests_with_allure.py [путь_к_тестам]
```

**Функционал:**
- Запускает указанные тесты через pytest
- Автоматически генерирует Allure отчеты
- Открывает отчет в браузере после выполнения

### 4. db_inspector.py
**Назначение:** Инспекция и проверка базы данных

**Использование:**
```bash
python scripts/maintenance/db_inspector.py
```

**Функционал:**
- Подключается к тестовой БД
- Выводит информацию о таблицах и данных
- Проверяет целостность данных

### 5. flexible_auth.py
**Назначение:** Гибкая авторизация с различными параметрами

**Использование:**
```bash
python scripts/maintenance/flexible_auth.py --user <username> --headless
```

**Параметры:**
- `--user` - имя пользователя для авторизации
- `--headless` - запуск в headless режиме
- `--save-cookies` - сохранить cookies после авторизации

## 📝 Примеры использования для Junior QA

### Пример 1: Проверка всех сохраненных cookies
```bash
# Запускаем тестер cookies
python scripts/maintenance/cookie_tester.py

# Выбираем опцию 1 для проверки всех файлов
# Смотрим статистику успешных авторизаций
```

### Пример 2: Авторизация новых пользователей
```bash
# Подготавливаем CSV файл с пользователями в secrets/
# Запускаем GUI авторизацию
python scripts/maintenance/run_csv_auth_gui.py

# Наблюдаем процесс в браузере
# Проверяем сохраненные cookies
```

### Пример 3: Быстрый запуск тестов с отчетом
```bash
# Запускаем все интеграционные тесты
python scripts/maintenance/run_tests_with_allure.py tests/integration/

# Отчет откроется автоматически в браузере
```

## ⚠️ Важные замечания

1. **Безопасность**: Все скрипты работают с чувствительными данными. Убедитесь, что:
   - Файлы с паролями находятся в `secrets/` и добавлены в `.gitignore`
   - Не выводите пароли в логи или консоль
   - Используйте переменные окружения для секретов

2. **Пути к файлам**: Скрипты ожидают запуска из корня проекта:
   ```bash
   cd D:\Bll_tests
   python scripts/maintenance/script_name.py
   ```

3. **Зависимости**: Перед запуском убедитесь, что установлены все зависимости:
   ```bash
   pip install -r requirements.txt
   playwright install
   ```

## 🔧 Решение проблем

### Ошибка "Модуль не найден"
```bash
# Убедитесь, что находитесь в корне проекта
cd D:\Bll_tests

# Проверьте PYTHONPATH
echo $env:PYTHONPATH
```

### Ошибка "Файл cookies не найден"
```bash
# Создайте папку cookies если её нет
mkdir cookies

# Запустите тест авторизации для создания cookies
pytest tests/auth/test_single_user_creds_login_and_save_cookie.py
```

### Браузер не открывается
```bash
# Переустановите браузеры Playwright
playwright install --force
```

## 📚 Дополнительная документация

- Детали о структуре cookies: см. `docs/guides/README_cookie_tester.md`
- Информация о тестах: см. `docs/GUIDES.md`
- Справочник по утилитам: см. `docs/REFERENCE.md`

---

_Этот документ поможет вам быстро начать использовать вспомогательные скрипты проекта. При возникновении вопросов обращайтесь к основной документации или к старшим коллегам._
