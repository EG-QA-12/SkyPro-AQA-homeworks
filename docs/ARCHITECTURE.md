# 🏗️ АРХИТЕКТУРА ФРЕЙМВОРКА АВТОТЕСТОВ

## 📋 ОБЗОР

Архитектура фреймворка автотестов спроектирована по принципам Clean Architecture с четким разделением ответственности между компонентами.

## 🗺️ СТРУКТУРА ПРОЕКТА

### Основные директории
```
bll_tests/
├── framework/                # Фреймворк автотестов (ядро)
│   ├── api/                  # API клиенты
│   ├── auth/                 # Система авторизации
│   ├── utils/                # Вспомогательные утилиты
│   ├── app/                  # Специфичный код приложения
│   ├── fixtures/             # Фикстуры pytest
│   └── test_bases/          # Базовые классы тестов
├── tests/                    # Тесты
│   ├── integration/          # Интеграционные тесты
│   ├── auth/                 # Тесты авторизации
│   ├── e2e/                  # E2E тесты
│   └── data/                # Тестовые данные
├── docs/                     # Документация
├── scripts/                 # Скрипты и утилиты
├── config/                  # Конфигурация
├── cookies/                 # Файлы кук
└── requirements.txt         # Зависимости
```

## 🏗️ ОСНОВНЫЕ КОМПОНЕНТЫ

### 1. API Клиенты (`framework/api/`)

#### BaseAPIClient
Базовый клиент для работы с API, предоставляющий:
- **Автоматическую авторизацию** и обновление кук
- **Обработку CSRF токенов** - автоматическое управление токенами
- **Retry логику** - повторные попытки при 401/419 ошибках
- **Типизированные ответы** - безопасная работа с API
- **Логирование** - подробные логи для диагностики

```python
from framework.api.base_client import BaseAPIClient

# Клиент автоматически авторизуется при создании
client = BaseAPIClient(base_url="https://expert.bll.by", role="admin")
response = client.get("/admin/posts")
```

#### AdminAPIClient
Специализированный клиент для административных операций:
- **Работа с панелью модерации** - получение данных модерации
- **Публикация вопросов и ответов** - создание контента
- **Взятие записей в работу** - управление модерацией
- **Поиск и фильтрация** - работа с большими объемами данных

```python
from framework.api.admin_client import AdminAPIClient

client = AdminAPIClient(role="admin")
entries = client.get_moderation_panel_data(limit=100)
```

### 2. Система авторизации (`framework/auth/`)

#### AuthManager
Основной менеджер авторизации с поддержкой:
- **Кэширования кук** - автоматическое сохранение и восстановление
- **Интеллектуального кэширования** - оптимальное управление временем жизни
- **Автоматического API-логина** - получение кук через учетные данные
- **Валидации сессий** - проверка актуальности кук

#### CookieProvider
Провайдер кук с поддержкой различных источников:
- **Переменные окружения** - `SESSION_COOKIE_{ROLE}`
- **Локальные файлы** - `cookies/{role}_session.txt`
- **API-логин** - автоматический логин через учетные данные

#### AuthUtils
Вспомогательные утилиты для работы с куками:
- **Валидация** - проверка формата и актуальности
- **Сохранение/загрузка** - работа с локальными файлами
- **Обработка ошибок** - централизованная логика исключений

### 3. Утилиты (`framework/utils/`)

#### HTML Parser
Парсер HTML для извлечения данных:
- **Парсинг панели модерации** - извлечение записей
- **Работа с CSRF токенами** - автоматическое получение токенов
- **Обработка таблиц** - извлечение табличных данных

#### Smart Auth Manager
Интеллектуальный менеджер авторизации:
- **Кэширование сессий** - оптимальное управление куками
- **Авто-ретрай на 401/419** - автоматическое обновление сессий
- **Поддержка нескольких ролей** - гибкая система ролей

#### Question Factory
Фабрика тестовых вопросов:
- **Генерация вопросов** - создание уникальных тестовых данных
- **Валидация вопросов** - проверка корректности данных
- **Поддержка категорий** - генерация по категориям

### 4. Базовые классы тестов (`framework/test_bases/`)

#### APITestBase
Базовый класс для API тестов:
- **Автоматическая авторизация** - клиенты с преднастроенными куками
- **Встроенные клиенты** - admin_client, moder_client, user_client
- **Фабрики данных** - question_factory, answer_factory
- **Логирование** - централизованное логирование

```python
from framework.test_bases.api_test_base import APITestBase

class TestQuestions(APITestBase):
    def test_create_question(self):
        # self.admin_client уже авторизован
        result = self.admin_client.create_test_question("Тестовый вопрос")
        assert result is True
```

#### UITestBase
Базовый класс для UI тестов:
- **Автоматическая авторизация** - контекст с куками
- **Playwright интеграция** - преднастроенные страницы
- **Скриншоты и логи** - автоматическое сохранение артефактов
- **Retry логика** - повторные попытки при ошибках

### 5. Фикстуры (`framework/fixtures/`)

#### AuthFixtures
Фикстуры для авторизации:
- **Автоматические клиенты** - admin_client, moder_client, user_client
- **Контексты Playwright** - авторизованные контексты
- **Фабрики данных** - question_factory, answer_factory

#### ReportFixtures
Фикстуры для отчетов:
- **Allure интеграция** - автоматические шаги и аттачменты
- **Скриншоты** - автоматическое сохранение скриншотов
- **Логи** - сохранение логов тестов

## 🔄 ПОТОКИ ДАННЫХ

### 1. API Тестирование
```
Test → APITestBase → BaseAPIClient → AuthManager → CookieProvider → API Endpoint
  ↑         ↑              ↑              ↓
  └───── Автоматическая авторизация ─────────────────────────────────────┘
```

### 2. UI Тестирование
```
Test → UITestBase → Playwright Context → AuthManager → CookieProvider → Browser
  ↑         ↑              ↑                 ↑              ↑            ↓
  └──────────────────── Авторизация в браузере ────────────────────────────┘
```

### 3. Получение кук
```
AuthManager
    ↓
CookieProvider (ENV → Files → API Login)
    ↓
Кука
    ↓
BaseAPIClient / Playwright Context
```

## 🎯 ПРИНЦИПЫ АРХИТЕКТУРЫ

### 1. Единая точка ответственности (Single Responsibility)
Каждый компонент имеет одну четкую ответственность:
- **AuthManager** - только управление авторизацией
- **BaseAPIClient** - только работа с API
- **HTML Parser** - только парсинг HTML
- **Question Factory** - только генерация вопросов

### 2. Открытость/Закрытость (Open/Closed)
Компоненты открыты для расширения, но закрыты для модификации:
```python
# Расширение функциональности через наследование
class ExtendedAdminClient(AdminAPIClient):
    def new_admin_feature(self):
        pass
```

### 3. Инверсия зависимостей (Dependency Inversion)
Высокоуровневые модули не зависят от низкоуровневых:
```python
# Высокоуровневый модуль зависит от абстракции
class APITestBase:
    def __init__(self):
        self.client = BaseAPIClient()  # Абстракция
```

### 4. Принцип инъекции зависимостей
Зависимости передаются через конструктор:
```python
class AuthManager:
    def __init__(self, cookie_provider=None):
        self.cookie_provider = cookie_provider or CookieProvider()
```

## 🛡️ БЕЗОПАСНОСТЬ

### 1. Управление секретами
- **Переменные окружения** - основной способ хранения кук
- **Локальные файлы** - альтернативное хранение вне репозитория
- **.gitignore** - защита от случайного коммита кук

### 2. Валидация данных
- **Автоматическая валидация кук** - проверка формата и актуальности
- **Типизированные ответы** - безопасная работа с API
- **Обработка ошибок** - централизованная логика исключений

### 3. Retry логика
- **Автоматический ретрай при 401/419** - обновление сессий
- **Экспоненциальная задержка** - предотвращение флуда
- **Лимит попыток** - предотвращение бесконечных циклов

## 🚀 ПРОИЗВОДИТЕЛЬНОСТЬ

### 1. Кэширование
- **Кэш кук** - минимизация API-логинов
- **Интеллектуальное обновление** - автоматическое обновление при истечении
- **Разделение по ролям** - отдельные кэши для разных ролей

### 2. Параллелизация
- **Поддержка pytest-xdist** - параллельный запуск тестов
- **Потокобезопасность** - безопасная работа в многопоточной среде
- **Разделение ресурсов** - изоляция тестов

### 3. Оптимизация сетевых запросов
- **Connection pooling** - переиспользование соединений
- **Keep-alive** - поддержание соединений
- **Компрессия** - gzip сжатие данных

## 🧪 ТЕСТИРОВАНИЕ

### 1. Архитектура тестов
- **Интеграционные тесты** - основная тестовая база
- **API тесты** - тестирование API клиентов
- **UI тесты** - тестирование пользовательского интерфейса
- **E2E тесты** - сквозные пользовательские сценарии

### 2. Базовые классы тестов
- **APITestBase** - для API тестов с автоматической авторизацией
- **UITestBase** - для UI тестов с авторизованным контекстом
- **Shared fixtures** - переиспользуемые фикстуры

### 3. Лучшие практики
- **Atomic tests** - независимые тесты без побочных эффектов
- **Clear naming** - понятные имена тестов и методов
- **Proper assertions** - четкие проверки ожидаемого результата
- **Meaningful logs** - информативные логи для диагностики

## 📚 ДОКУМЕНТАЦИЯ

### 1. Структура документации
- **Главная страница** - `docs/README.md`
- **Быстрый старт** - `docs/GETTING_STARTED.md`
- **Архитектура** - `docs/ARCHITECTURE.md`
- **Компоненты** - `docs/COMPONENTS/`
- **Тестирование** - `docs/TESTING/`
- **Инструменты** - `docs/TOOLS/`
- **Миграция** - `docs/MIGRATION/`
- **Справочники** - `docs/REFERENCES/`

### 2. Навигация
- **Четкая структура** - логическая организация документов
- **Перекрестные ссылки** - связь между документами
- **Поиск по ключевым словам** - быстрый доступ к информации
- **Примеры использования** - практические руководства

## 🤝 ПОДДЕРЖКА И РАЗВИТИЕ

### 1. Обратная связь
- **GitHub Issues** - сообщения об ошибках и предложения
- **Code Review** - участие в улучшении кода
- **Документация** - помощь в написании руководств

### 2. Развитие
- **Регулярные обновления** - улучшение функциональности
- **Обратная совместимость** - сохранение старых интерфейсов
- **Тестирование изменений** - автоматические тесты новых функций
- **Документирование** - актуальная документация

## 📊 МЕТРИКИ И МОНИТОРИНГ

### 1. Производительность
- **Время запуска тестов** - мониторинг скорости выполнения
- **Успешность тестов** - отслеживание flaky тестов
- **Использование ресурсов** - CPU, память, сеть

### 2. Качество кода
- **Покрытие тестами** - автоматический подсчет покрытия
- **Код стайл** - автоматическая проверка стиля кода
- **Статический анализ** - выявление потенциальных проблем

### 3. Надежность
- **Retry rate** - частота повторных попыток
- **Error rate** - частота ошибок
- **Success rate** - процент успешных выполнений

## 🔧 РЕШЕНИЕ ПРОБЛЕМ

### 1. Диагностика
- **Логирование** - подробные логи для диагностики
- **Скриншоты** - визуальная диагностика UI проблем
- **Артефакты** - сохранение данных для анализа

### 2. Отладка
- **Debug режим** - подробный вывод для отладки
- **Step-by-step выполнение** - пошаговое выполнение тестов
- **Интерактивная отладка** - возможность ручного управления

### 3. Восстановление
- **Retry логика** - автоматическое восстановление при ошибках
- **Fallback механизмы** - резервные способы выполнения
- **Graceful degradation** - мягкое понижение функциональности
