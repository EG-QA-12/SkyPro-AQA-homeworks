# 🚀 БЫСТРЫЙ СТАРТ С ФРЕЙМВОРКОМ АВТОТЕСТОВ

## 📋 ПРЕДВАРИТЕЛЬНЫЕ ТРЕБОВАНИЯ

### Системные требования
- **Python 3.8+** (рекомендуется Python 3.11+)
- **Windows 10/11**, **macOS 10.15+**, или **Ubuntu 20.04+**
- **Git** для управления версиями
- **Visual Studio Code** (рекомендуется) или другой IDE

### Рекомендуемые инструменты
- **Node.js 16+** (для Playwright)
- **Docker** (опционально, для контейнеризации)
- **Allure CLI** (для генерации отчетов)

## 📥 УСТАНОВКА

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd bll_tests
```

### 2. Создание виртуального окружения
```bash
# Windows
python -m venv .venv
.venv\Scripts\activate

# macOS/Linux
python3 -m venv .venv
source .venv/bin/activate
```

### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 4. Установка браузеров для Playwright
```bash
playwright install
```

## ⚙️ НАСТРОЙКА ОКРУЖЕНИЯ

### Переменные окружения
Создайте файл `.env` в корневой директории:

```env
# Обязательные переменные
SESSION_COOKIE_ADMIN=ваша_админская_кука
SESSION_COOKIE_MODERATOR=ваша_кука_модератора
SESSION_COOKIE_USER=ваша_пользовательская_кука

# Альтернатива: одна кука для всех ролей
# SESSION_COOKIE=общая_кука

# API учетные данные (опционально, для автоматического логина)
API_USERNAME_ADMIN=admin@example.com
API_PASSWORD_ADMIN=ваш_пароль
API_USERNAME_MODERATOR=moder@example.com
API_PASSWORD_MODERATOR=ваш_пароль
API_USERNAME_USER=user@example.com
API_PASSWORD_USER=ваш_пароль
```

### Структура файлов кук
Альтернативно, можно использовать локальные файлы:

```
cookies/
├── admin_session.txt          # Текстовый файл с кукой
├── moderator_session.txt      # Текстовый файл с кукой
├── user_session.txt           # Текстовый файл с кукой
├── admin_cookies.json         # JSON файл Playwright формата
├── moderator_cookies.json     # JSON файл Playwright формата
└── user_cookies.json          # JSON файл Playwright формата
```

## 🧪 ПЕРВЫЙ ЗАПУСК ТЕСТОВ

### Запуск smoke тестов
```bash
# Быстрый запуск smoke-тестов (15-20 секунд)
pytest tests/smoke/ -v

# Только бургер-меню (быстрые тесты)
pytest tests/smoke/burger_menu/left_column/ -v

# Бургер-меню с визуальными паузами
pytest tests/smoke/burger_menu/right_column/ --headed -v

# Запуск с параллелизацией
pytest tests/smoke/ -n 4 -v
```

### Запуск Playwright тестов
```bash
# Запуск интеграционных UI тестов
pytest tests/integration/test_main_page_links.py -v

# Запуск с генерацией Allure отчета
pytest tests/smoke/burger_menu/ --alluredir=allure-results -v
```

### Запуск API тестов
```bash
# Тесты авторизации
pytest tests/auth/test_api_mass_authorization.py -v

# Тесты публикации вопросов
pytest tests/integration/test_publish_question_api.py -v
```

## 📊 ПРОСМОТР ОТЧЕТОВ

### Просмотр Allure отчета
```bash
# Генерация и открытие отчета
allure serve allure-results
```

### Просмотр результатов в консоли
```bash
# Подробный вывод
pytest tests/integration/test_auth_quick.py -v

# Вывод с логами
pytest tests/integration/test_auth_quick.py -v --tb=short -s
```

## 🛠️ ОСНОВНЫЕ КОМАНДЫ

### Разработка
```bash
# Запуск одного теста с логами
pytest tests/integration/test_auth_quick.py::test_admin_can_access_moderation -v -s

# Запуск тестов с определенным маркером
pytest -m "api" -v

# Запуск тестов с исключением медленных
pytest -m "not slow" -v
```

### Отладка
```bash
# Запуск с отладочными логами
pytest tests/integration/test_auth_quick.py --log-cli-level=DEBUG

# Запуск с сохранением скриншотов
pytest tests/integration/test_burger_menu_links_playwright.py --screenshot=on
```

### CI/CD
```bash
# Параллельный запуск для CI
pytest -n 8 --dist=loadfile -v

# Запуск с покрытием кода
pytest --cov=framework --cov-report=html -v
```

## 🎯 ПЕРВЫЙ ТЕСТ

### Создание простого API теста
```python
# tests/integration/test_my_first_test.py
import pytest
from framework.test_bases.api_test_base import APITestBase

class TestMyFirstTest(APITestBase):
    """Мой первый тестовый класс."""
    
    def test_admin_can_create_question(self):
        """Администратор может создать вопрос."""
        # Используем встроенный клиент
        result = self.admin_client.create_test_question("Мой первый тестовый вопрос")
        assert result is True
```

### Создание простого UI теста
```python
# tests/integration/test_my_first_ui_test.py
import pytest
from framework.test_bases.ui_test_base import UITestBase

class TestMyFirstUITest(UITestBase):
    """Мой первый UI тестовый класс."""
    
    def test_can_navigate_to_main_page(self, page):
        """Можно перейти на главную страницу."""
        page.goto("https://bll.by")
        assert page.title() == "BLL - Главная страница"
```

## 📁 СТРУКТУРА ПРОЕКТА

```
bll_tests/
├── framework/                # Фреймворк автотестов
│   ├── api/                  # API клиенты
│   ├── auth/                 # Система авторизации
│   ├── utils/                # Вспомогательные утилиты
│   └── test_bases/          # Базовые классы тестов
├── tests/                    # Тесты
│   ├── smoke/               # Smoke-тесты (критически важные)
│   │   └── burger_menu/     # Тесты бургер-меню
│   │       ├── left_column/ # Быстрые тесты левой колонки
│   │       ├── right_column/# Тесты правой колонки с паузами
│   │       └── pages/       # Page Object классы
│   ├── integration/          # Интеграционные тесты
│   ├── auth/                # Тесты авторизации
│   └── e2e/                 # E2E инфраструктура
├── docs/                     # Документация
├── scripts/                 # Скрипты и утилиты
├── cookies/                 # Файлы кук
├── allure-results/         # Результаты Allure
└── requirements.txt         # Зависимости
```

## 🔧 РЕШЕНИЕ ЧАСТЫХ ПРОБЛЕМ

### Проблема 1: "Invalid session cookie"
**Решение:** Проверьте валидность куки в файле `.env` или `cookies/admin_session.txt`

### Проблема 2: "Browser not found"
**Решение:** Установите браузеры для Playwright:
```bash
playwright install
```

### Проблема 3: "Module not found"
**Решение:** Убедитесь что виртуальное окружение активировано:
```bash
# Windows
.venv\Scripts\activate

# macOS/Linux
source .venv/bin/activate
```

### Проблема 4: "Permission denied"
**Решение:** Запустите командную строку от имени администратора (Windows) или используйте sudo (macOS/Linux)

## 📚 ДАЛЬНЕЙШЕЕ ОБУЧЕНИЕ

### Рекомендуемые следующие шаги:
1. [Архитектура проекта](ARCHITECTURE.md) - понимание структуры фреймворка
2. [Система авторизации](COMPONENTS/AUTH_SYSTEM.md) - работа с куками и сессиями
3. [Написание тестов](TESTING/WRITING_TESTS.md) - создание сложных тестовых сценариев
4. [Лучшие практики](TESTING/BEST_PRACTICES.md) - советы по эффективному тестированию

### Полезные ресурсы:
- [Примеры тестов](REFERENCES/EXAMPLES.md) - практические примеры
- [API справочник](REFERENCES/API_REFERENCE.md) - полное описание API клиентов
- [FAQ](REFERENCES/FAQ.md) - часто задаваемые вопросы

## 🤝 ПОДДЕРЖКА

При возникновении проблем:
1. Проверьте [часто задаваемые вопросы](REFERENCES/FAQ.md)
2. Изучите [примеры](REFERENCES/EXAMPLES.md)
3. Создайте issue в репозитории
4. Обратитесь к Lead SDET Architect

**Удачи в автоматизации тестирования! 🚀**
