# Playwright Codegen для Python: Полное руководство

## 📋 Обзор

Playwright Codegen — это мощный инструмент для быстрой автоматической генерации тестов на Python. Вместо того чтобы писать код вручную, вы просто взаимодействуете с веб-сайтом в браузере, а Codegen записывает ваши действия и преобразует их в готовый Python-скрипт с использованием Playwright API.

## 🚀 Основные команды

### Базовый запуск

```bash
# Запуск Codegen для Python и открытие сайта
playwright codegen --target python https://bll.by
```

Эта команда:
- Запускает браузер (по умолчанию Chromium)
- Открывает Playwright Inspector для записи действий
- Генерирует код на Python в реальном времени

### Выбор браузера

```bash
# Запуск в Firefox
playwright codegen --target python --browser firefox https://bll.by

# Запуск в WebKit (Safari)
playwright codegen --target python --browser webkit https://bll.by

# Запуск в Chromium (по умолчанию)
playwright codegen --target python --browser chromium https://bll.by
```

### Сохранение в файл

```bash
# Сгенерировать код и сразу сохранить его в файл
playwright codegen --target python -o my_test.py https://bll.by
```

### Эмуляция устройств

```bash
# Эмуляция мобильного устройства
playwright codegen --target python --device="iPhone 13" https://bll.by

# Эмуляция планшета
playwright codegen --target python --device="iPad Pro" https://bll.by

# Настройка размера экрана
playwright codegen --target python --viewport-size=1920,1080 https://bll.by

# Темная тема
playwright codegen --target python --color-scheme=dark https://bll.by
```

## 🔐 Работа с аутентификацией

Codegen позволяет сохранять и повторно использовать состояние сессии (cookies, localStorage), чтобы не проходить аутентификацию при каждой записи теста.

```bash
# Шаг 1: Войдите на сайт и сохраните состояние аутентификации
playwright codegen --target python --save-storage=auth.json https://github.com/login

# Шаг 2: Используйте сохраненное состояние для записи тестов
playwright codegen --target python --load-storage=auth.json https://github.com/microsoft/playwright
```

## 📱 Практические примеры

### Пример 1: Полный сценарий с сохранением

Этот пример демонстрирует запуск Codegen для сайта bll.by с эмуляцией iPhone 13, темной темой и автоматическим сохранением результата в файл mobile_test.py.

```bash
playwright codegen \
  --target python \
  --device="iPhone 13" \
  --color-scheme=dark \
  -o mobile_test.py \
  https://bll.by
```

### Пример 2: Тестирование с состоянием входа

Здесь показано, как записать состояние входа в систему, а затем использовать его для генерации теста на странице, требующей аутентификации.

```bash
# Сначала сохраняем сессию после входа
playwright codegen --target python --save-storage=app_auth.json https://app.example.com/login

# Затем используем сохраненную сессию для записи теста на главной панели
playwright codegen --target python --load-storage=app_auth.json -o dashboard_test.py https://app.example.com/dashboard
```

### Пример 3: Кросс-браузерное тестирование

```bash
# Тестирование в разных браузерах
playwright codegen --target python --browser chromium -o chrome_test.py https://bll.by
playwright codegen --target python --browser firefox -o firefox_test.py https://bll.by
playwright codegen --target python --browser webkit -o safari_test.py https://bll.by
```

## 📝 Пример сгенерированного Python-кода

После записи ваших действий Codegen создаст примерно такой скрипт, который можно сразу запустить с помощью pytest.

```python
import re
from playwright.sync_api import Page, expect

def test_example(page: Page) -> None:
    # Переходим на сайт
    page.goto("https://bll.by/")

    # Кликаем по ссылке "Контакты"
    page.get_by_role("link", name="Контакты").click()

    # Проверяем, что на странице виден заголовок "Контакты"
    expect(page.get_by_role("heading", name="Контакты")).to_be_visible()

    # Заполняем поле "Ваше имя"
    page.get_by_placeholder("Ваше имя").fill("Тест")

    # Выбираем опцию из выпадающего списка
    page.get_by_label("Тема обращения").select_option("Техническая поддержка")

    # Вводим текст в многострочное поле
    page.get_by_label("Сообщение").fill("Это тестовое сообщение от Playwright Codegen")

    # Кликаем по кнопке отправки
    page.get_by_role("button", name="Отправить").click()

    # Проверяем успешную отправку
    expect(page.get_by_text("Спасибо за обращение!")).to_be_visible()
```

## ⚙️ Дополнительные возможности

### Работа с локаторами

Codegen автоматически генерирует устойчивые локаторы, используя приоритеты:
1. `data-testid` атрибуты (самый надежный)
2. ARIA роли и лейблы
3. Текст элементов
4. CSS селекторы (крайний случай)

### Ожидания и синхронизация

Codegen автоматически добавляет необходимые ожидания:
- `wait_for_load_state('networkidle')` для полной загрузки страницы
- `wait_for_selector()` для элементов
- Автоматические ожидания в assertions

### Запись сложных сценариев

- Навигация по страницам
- Взаимодействие с формами
- Работа с модальными окнами
- Обработка алертов и диалогов
- Скроллинг и взаимодействие с динамическим контентом

## 🔧 Интеграция с вашим проектом

### Структура тестов

Сгенерированные тесты можно легко интегрировать в существующую структуру:

```
tests/
├── integration/
│   ├── generated/
│   │   ├── mobile_tests.py      # Сгенерированные мобильные тесты
│   │   ├── desktop_tests.py     # Десктопные тесты
│   │   └── auth_tests.py        # Тесты с аутентификацией
```

### Запуск сгенерированных тестов

```bash
# Запуск всех сгенерированных тестов
pytest tests/integration/generated/ -v

# Запуск с Allure отчетами
pytest tests/integration/generated/ --alluredir=allure-results -v
allure serve allure-results
```

### Доработка сгенерированного кода

Codegen создает базовый код, который рекомендуется дорабатывать:

1. **Добавление assertions**: Усилить проверки с помощью `expect()`
2. **Параметризация**: Использовать `@pytest.mark.parametrize` для разных данных
3. **Фикстуры**: Интегрировать с существующими pytest фикстурами
4. **Обработка ошибок**: Добавить try/except блоки
5. **Логирование**: Добавить allure шаги и описания

## 🎯 Лучшие практики

### 1. Используйте осмысленные имена файлов
```bash
# Хорошо
playwright codegen -o burger_menu_navigation_test.py https://bll.by

# Плохо
playwright codegen -o test1.py https://bll.by
```

### 2. Сохраняйте состояние аутентификации
```bash
# Создайте отдельный файл с аутентификацией
playwright codegen --save-storage=user_session.json https://bll.by/login
```

### 3. Тестируйте в разных браузерах
```bash
# Запустите Codegen в каждом браузере для выявления incompatibilities
playwright codegen --browser chromium -o chrome_test.py https://bll.by
playwright codegen --browser firefox -o firefox_test.py https://bll.by
```

### 4. Используйте device emulation для мобильного тестирования
```bash
# Тестируйте на реальных устройствах
playwright codegen --device="iPhone 13" -o mobile_test.py https://bll.by
```

## 🚨 Важные замечания

- **Codegen не заменяет ручное написание тестов**: Он создает базовый код, который нужно дорабатывать
- **Проверяйте сгенерированные локаторы**: Убедитесь, что они надежны и не сломаются при изменениях UI
- **Добавляйте assertions**: Codegen записывает действия, но не всегда добавляет проверки результатов
- **Тестируйте на реальных данных**: Избегайте использования тестовых данных в продакшене

## 📚 Дополнительные ресурсы

- [Официальная документация Playwright Codegen](https://playwright.dev/python/docs/codegen)
- [Playwright CLI команды](https://playwright.dev/python/docs/cli)
- [Лучшие практики тестирования](../TESTING/BEST_PRACTICES.md)
- [Написание тестов](../TESTING/WRITING_TESTS.md)