# 🍪 Массовый тестер кук test_joint_session

> Перемещено из корня проекта 01-Jul-2025. Исторически файл лежал в `README_cookie_tester.md`, теперь вся справочная документация о вспомогательных скриптах хранится в `docs/guides/`.

(ниже приведено неизменённое содержимое оригинального файла)

# 🍪 Массовый тестер кук test_joint_session

## Описание
Скрипт `cookie_tester.py` автоматизирует процесс проверки авторизации пользователей на сайте ca.bll.by с использованием кук `test_joint_session`, сохраненных в JSON-файлах.

## Возможности скрипта
- ✅ Массовая обработка файлов с куками
- 🎯 Выборочная обработка (диапазон или конкретные файлы)
- 🔍 Автоматическое извлечение куки `test_joint_session`
- 🌐 Проверка авторизации на ca.bll.by
- 📊 Детальная статистика результатов
- 🛡️ Обработка ошибок и исключений

## Требования
```bash
pip install requests
```

## Как использовать

### 1. Запуск скрипта
```bash
python cookie_tester.py
```

### 2. Выбор файлов для обработки
Скрипт предложит 5 вариантов:

1. **Обработать ВСЕ файлы** - проверит все JSON-файлы в папке
2. **Обработать ДИАПАЗОН** - например, файлы с 1 по 50
3. **Обработать КОНКРЕТНЫЕ файлы** - например, файлы 1,5,10,25
4. **Показать список файлов** - посмотреть все доступные файлы
5. **Выход** - завершить работу

### 3. Примеры использования

#### Проверить все файлы:
```
Выберите опцию: 1
```

#### Проверить файлы с 1 по 50:
```
Выберите опцию: 2
Введите диапазон: 1-50
```

#### Проверить конкретные файлы:
```
Выберите опцию: 3
Введите номера файлов: 1,5,10,25,100
```

## Что показывает результат

### Для каждого файла:
- ✅ Кука найдена или ⚠️ кука отсутствует
- ✅ Авторизация успешна или ❌ авторизация не удалась
- Детали ответа сервера

### Итоговая статистика:
- 📁 Общее количество обработанных файлов
- 🍪 Файлы с найденной кукой test_joint_session
- ✅ Количество успешных авторизаций
- ❌ Количество неудачных авторизаций
- 📈 Процент успеха
- 🎉 Список файлов с успешной авторизацией

## Пример вывода
```
🚀 НАЧИНАЕМ МАССОВОЕ ТЕСТИРОВАНИЕ
📂 Файлов к обработке: 10

[1/10] 🔍 Обрабатываем: test_user1_cookies.json
  ✅ Кука найдена, value: eyJpdiI6IlYrQUZVNWR1Z292VUplbWxJOVJHY3c9PSIsInZhbHVl...
  ✅ Авторизация успешна (код: 200)

[2/10] 🔍 Обрабатываем: admin_cookies.json
  ⚠️  Кука 'test_joint_session' не найдена

===============================================================================
📊 ИТОГОВАЯ СТАТИСТИКА
===============================================================================
📁 Всего файлов обработано: 10
🍪 Файлов с кукой 'test_joint_session': 5
✅ Успешных авторизаций: 3
❌ Неудачных авторизаций: 2
⚠️  Файлов без нужной куки: 5
📈 Процент успешных авторизаций: 60.0%

🎉 ФАЙЛЫ С УСПЕШНОЙ АВТОРИЗАЦИЕЙ:
  ✅ test_user1_cookies.json
  ✅ test_user2_cookies.json
  ✅ subscription2_user_cookies.json
```

## Устранение неполадок

### Ошибка "Папка с куками не найдена"
Убедитесь, что папка `D:\Bll_tests\cookies` существует и содержит JSON-файлы.

### Ошибка "Ошибка соединения"
Проверьте:
- Подключение к интернету
- Доступность сайта ca.bll.by
- Настройки прокси/брандмауэра

### Ошибка чтения JSON-файла
Проверьте корректность формата JSON в файлах кук.

## Для разработчиков

### Структура кода:
- `CookieTester` - основной класс
- `get_cookie_files()` - получение списка файлов
- `extract_target_cookie()` - извлечение куки из JSON
- `test_cookie_authorization()` - проверка авторизации
- `display_file_menu()` - интерактивное меню

### Настройка проверки авторизации:
В методе `test_cookie_authorization()` можно изменить индикаторы авторизации:
```python
auth_indicators = [
    'logout',     # Кнопка выхода
    'profile',    # Профиль пользователя
    'dashboard',  # Панель управления
    'Выйти',      # Кнопка выхода на русском
    'Профиль'     # Профиль на русском
]
```

## Безопасность
- Скрипт не сохраняет куки или логины
- Все запросы выполняются в изолированной сессии
- Автоматическая очистка кук между тестами
