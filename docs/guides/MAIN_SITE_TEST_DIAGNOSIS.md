# Диагностика проблемы с тестом Main сайта (bll.by)

## 📋 Проблема
Тест авторизации на главной странице bll.by не работает. Метод `test_main_site_authorization` падает на этапе поиска элементов.

## 🔍 Анализ текущего кода

### Текущая логика теста:
1. Переход на `https://bll.by`
2. Поиск ссылки "Бонусы": `a[href="https://bonus.bll.by"]`
3. Клик по ссылке
4. Проверка перехода на `bonus.bll.by`
5. Поиск элемента "КАТАЛОГ БОНУСОВ": `a[href="https://bonus.bll.by/shop"].lnk-bonus_2`

## 🚨 Возможные причины проблемы:

### 1. Неправильные селекторы
- Ссылка "Бонусы" может иметь другой `href` или дополнительные атрибуты
- Элемент "КАТАЛОГ БОНУСОВ" может иметь другой класс или структуру

### 2. Динамическая загрузка
- Элементы могут загружаться AJAX
- Нужно увеличить время ожидания или добавить проверки видимости

### 3. Изменения в DOM
- Структура страницы могла измениться
- Элементы могут быть в другом контейнере

### 4. Проблемы с авторизацией
- SSO куки могут не работать на bll.by
- Может требоваться дополнительная авторизация

## 🛠️ Рекомендации по исправлению:

### 1. Использовать Playwright Codegen для получения правильных селекторов
```bash
# Запустить codegen для анализа страницы
npx playwright codegen https://bll.by
```

### 2. Добавить отладочную информацию
- Логировать все найденные ссылки на странице
- Показывать HTML структуры проблемных элементов
- Добавить скриншоты для визуального анализа

### 3. Улучшить селекторы
```python
# Вместо точного href использовать более гибкие селекторы
bonuses_link = page.locator('a').filter(has_text='Бонусы').first

# Или искать по частичному совпадению
bonuses_link = page.locator('a[href*="bonus.bll.by"]').first
```

### 4. Добавить проверки состояния страницы
```python
# Проверить что страница полностью загружена
page.wait_for_load_state('networkidle')

# Проверить наличие авторизации
profile_element = page.locator('[class*="profile"]').first
if profile_element.is_visible():
    logger.info("✅ Пользователь авторизован на главной странице")
```

## 📝 План исправления:

1. **Создать диагностический скрипт** для анализа DOM структуры
2. **Запустить codegen** для получения актуальных селекторов
3. **Добавить fallback селекторы** с разными вариантами поиска
4. **Увеличить таймауты** и добавить повторные попытки
5. **Протестировать** исправления на реальной странице

## 🎯 Следующие шаги:

1. Запустить codegen для анализа текущей структуры страницы
2. Создать отладочный вывод всех ссылок и элементов
3. Протестировать альтернативные селекторы
4. Внедрить исправления в основной код