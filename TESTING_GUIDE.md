# 🧪 РУКОВОДСТВО ПО ТЕСТИРОВАНИЮ

## 📋 Обзор тестов в проекте

Найдено **57 тестов** в различных категориях:

### 🏗️ Структура тестов:
```
tests/
├── auth/                     # Тесты авторизации (19 тестов)
├── e2e/                      # End-to-end тесты (22 теста)
│   ├── redirect_tests/       # Тесты редиректов (10 тестов)
│   └── user_journeys/        # Пользовательские сценарии (4 теста)
└── integration/              # Интеграционные тесты (10 тестов)
    └── infrastructure/       # Инфраструктурные тесты (5 тестов)
```

## ⚙️ Настройка окружения для тестирования

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
playwright install
```

### 2. Настройка переменных окружения (ОБЯЗАТЕЛЬНО)
```powershell
# PowerShell
$env:AUTH_USERNAME = "test_user"
$env:AUTH_PASSWORD = "test_password" 
$env:AUTH_DOMAIN = "bll.by"
$env:TEST_ENVIRONMENT = "dev"
```

```bash
# Bash
export AUTH_USERNAME="test_user"
export AUTH_PASSWORD="test_password"
export AUTH_DOMAIN="bll.by"
export TEST_ENVIRONMENT="dev"
```

### 3. Альтернатива: Создайте файл .env
```bash
# Скопируйте шаблон
cp config/.env.template config/.env
# Отредактируйте config/.env с реальными данными
```

## 🚀 Способы запуска тестов

### 📊 1. Проверка коллекции тестов
```bash
python -m pytest --collect-only --quiet
```
**Результат**: список всех найденных тестов

### 🔥 2. Быстрые тесты (без браузера)
```bash
# Тесты подключения к сайтам
python -m pytest tests/e2e/redirect_tests/test_redirect_status_codes.py::test_url_connectivity -v

# Все инфраструктурные тесты
python -m pytest tests/integration/infrastructure/ -v
```

### 🌐 3. Тесты авторизации 
```bash
# Все тесты авторизации через cookies
python -m pytest tests/auth/ -v

# Конкретный тест с одним cookie файлом
python -m pytest tests/auth/test_authentication.py::test_login_with_cookie[chromium-admin_cookies.json] -v
```

### 🎯 4. E2E тесты
```bash
# Тесты cookie авторизации
python -m pytest tests/e2e/test_cookie_auth.py -v

# Демонстрационные тесты новой архитектуры
python -m pytest tests/e2e/test_updated_auth_demo.py -v

# Тесты пользовательских путешествий
python -m pytest tests/e2e/user_journeys/ -v
```

### 🔄 5. Все тесты по категориям
```bash
# Все быстрые тесты (без браузера)
python -m pytest tests/e2e/redirect_tests/ tests/integration/infrastructure/ -v

# Все тесты с браузером
python -m pytest tests/auth/ tests/e2e/test_cookie_auth.py -v

# Абсолютно все тесты (ДОЛГО!)
python -m pytest -v
```

## 🎛️ Полезные параметры pytest

### 📝 Подробный вывод
```bash
python -m pytest -v -s  # -v подробно, -s показывать print()
```

### 🏷️ Запуск по меткам
```bash
python -m pytest -m auth           # Только тесты авторизации
python -m pytest -m demo           # Только демонстрационные тесты
python -m pytest -m smoke          # Только smoke тесты
```

### 🛑 Остановка на первой ошибке
```bash
python -m pytest -x                # Остановка на первом fail
python -m pytest --maxfail=3       # Остановка после 3 fails
```

### 🔍 Фильтрация по имени
```bash
python -m pytest -k "cookie"       # Тесты содержащие "cookie"
python -m pytest -k "not redirect" # Исключить тесты с "redirect"
```

### ⚡ Параллельный запуск
```bash
pip install pytest-xdist
python -m pytest -n 4              # Запуск в 4 потока
```

## 📊 Отчеты и результаты

### 📈 HTML отчет
```bash
pip install pytest-html
python -m pytest --html=report.html --self-contained-html
```

### 🎨 Allure отчет
```bash
pip install allure-pytest
python -m pytest --alluredir=allure-results
allure serve allure-results
```

### 📱 JUnit XML
```bash
python -m pytest --junit-xml=junit.xml
```

## 🚨 Распространенные проблемы и решения

### ❌ Проблема: "ValueError: Обязательная переменная окружения 'AUTH_USERNAME' не найдена"
**Решение**: Установите переменные окружения как указано выше

### ❌ Проблема: "playwright._impl._errors.Error: Browser is not connected"
**Решение**: 
```bash
playwright install
```

### ❌ Проблема: Тесты падают по таймауту
**Решение**: Увеличьте таймаут:
```bash
$env:BROWSER_TIMEOUT = "60000"  # 60 секунд
```

### ❌ Проблема: Файлы cookies не найдены
**Решение**: Тесты используют тестовых пользователей по умолчанию если файлы не найдены

## 📋 Рекомендуемая последовательность тестирования

### 🥇 1. Первичная проверка (быстро)
```bash
# Проверка сбора тестов
python -m pytest --collect-only --quiet

# Быстрые тесты подключения
python -m pytest tests/e2e/redirect_tests/test_redirect_status_codes.py::test_url_connectivity -v
```

### 🥈 2. Функциональная проверка (средне)
```bash
# Тесты без браузера
python -m pytest tests/integration/infrastructure/ -v

# Базовые тесты авторизации
python -m pytest tests/e2e/test_cookie_auth.py -v
```

### 🥉 3. Полная проверка (долго)
```bash
# Все тесты авторизации
python -m pytest tests/auth/ -v

# Демонстрационные тесты
python -m pytest tests/e2e/test_updated_auth_demo.py -v
```

## 🔧 Конфигурация

### pytest.ini
Используется файл `tests/e2e/pytest.ini` с настройками:
- Playwright browser: chromium
- Marker definitions
- Test paths

### conftest.py
Настройки fixtures в:
- `tests/conftest.py` - основные fixtures
- `tests/e2e/conftest.py` - E2E fixtures  
- `tests/integration/conftest.py` - интеграционные fixtures

## 📈 Метрики и покрытие

### Подсчет тестов по категориям:
- **Auth tests**: 19 тестов (один файл с параметризацией)
- **E2E tests**: 22 теста (различные сценарии)
- **Infrastructure tests**: 10 тестов (подключения и редиректы)
- **Integration tests**: 6 тестов (интеграционная логика)

### Время выполнения (приблизительно):
- **Быстрые тесты**: ~2-5 секунд
- **Тесты с браузером**: ~10-30 секунд за тест
- **Все тесты**: ~15-30 минут

---
**💡 Совет**: Начинайте с быстрых тестов, затем переходите к более сложным!

