# 🚀 Быстрый запуск тестов

## ⚡ Самые быстрые команды

### Windows (батники):
- **`run_tests_parallel.bat`** - подробный вывод (~20 сек)
- **`run_tests_quick.bat`** - краткий вывод (~15 сек) 
- **`run_tests_allure.bat`** - с Allure отчетами

### Командная строка:
```bash
# Оптимальный вариант (подробно):
python -m pytest -n 10 -v

# Максимально быстро (кратко):
python -m pytest -n 10 -q

# Автоматическое количество потоков:
python -m pytest -n auto -v
```

## 📊 Статистика производительности

| Команда | Время | Детализация | Рекомендация |
|---------|-------|-------------|--------------|
| `pytest -n 10 -v` | ~20 сек | Подробная | ✅ **Основная** |
| `pytest -n 10 -q` | ~15 сек | Краткая | ⚡ **Самая быстрая** |
| `pytest -v` | ~44 сек | Подробная | 🐌 Медленно |

## 🎯 Специальные запуски

```bash
# Только API авторизация (включена в основные тесты):
python -m pytest tests/auth/ -n 10 -v

# Только интеграционные:
python -m pytest tests/integration/ -n 10 -v

# Только инфраструктурные:
python -m pytest tests/e2e/ -n 10 -v

# С Allure отчетами:
python -m pytest -n 10 -v --alluredir=allure-results
```

## 📈 Результат оптимизации
- **26 стабильных тестов** (было 46+)
- **100% успешность**
- **Ускорение в 30 раз** (от 10 мин до 20 сек!)
- **Параллельное выполнение** в 10 потоков

## 📊 Профилирование и оптимизация

Для анализа производительности тестов и выявления узких мест можно использовать следующие подходы:

### Профилирование времени выполнения:
```bash
# Запуск с замером времени выполнения каждого теста
python -m pytest --durations=10 -v

# Подробный замер времени выполнения
python -m pytest --durations=0 -v
```

### Профилирование с использованием cProfile:
```bash
# Профилирование конкретного теста
python -m cProfile -o test_profile.prof -m pytest tests/integration/test_example.py

# Анализ результатов профилирования
python -m pstats test_profile.prof
```

### Мониторинг использования ресурсов:
```bash
# Мониторинг CPU и памяти во время выполнения тестов
python -m pytest &
pid=$!
while kill -0 $pid 2>/dev/null; do
  echo "CPU: $(ps -p $pid -o %cpu=)%, MEM: $(ps -p $pid -o %mem=)%"
  sleep 1
done
```

### Оптимизация тестов:
1. **Минимизация использования time.sleep()** - заменить на явные ожидания
2. **Использование фикстур с правильной областью видимости** - избегать повторной инициализации
3. **Кэширование данных** - повторное использование подготовленных данных
4. **Параллельное выполнение** - использование pytest-xdist для распараллеливания
5. **Оптимизация работы с базой данных** - использование транзакций и отката изменений

## 🛠️ Отладка тестов

### Отладка с использованием pdb:
```bash
# Запуск теста с отладчиком
python -m pytest --pdb tests/integration/test_example.py

# Отладка только при ошибках
python -m pytest --pdb-trace tests/integration/test_example.py
```

### Логирование для отладки:
```bash
# Включение подробного логирования
python -m pytest -v --log-cli-level=DEBUG

# Сохранение логов в файл
python -m pytest -v --log-file=test_run.log --log-file-level=DEBUG
```